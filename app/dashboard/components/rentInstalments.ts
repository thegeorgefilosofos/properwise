// ═══════════════════════════════════════════════════════════════════════════
// ΟΙ ΔΟΣΕΙΣ ΓΕΝΝΙΟΥΝΤΑΙ ΟΤΑΝ ΓΡΑΦΕΤΑΙ Η ΜΙΣΘΩΣΗ, ΟΧΙ ΟΤΑΝ ΤΙΣ ΚΟΙΤΑΞΕΙΣ
// ─────────────────────────────────────────────────────────────────────────
// ΤΟ ΣΦΑΛΜΑ, ΟΠΩΣ ΤΟ ΖΟΥΣΕ Ο ΙΔΙΟΚΤΗΤΗΣ. Η δημιουργία των δόσεων ζούσε μέσα σε
// ένα `useEffect` της `PaymentsView` — της οθόνης που εμφανίζεται ΜΟΝΟ όταν
// ανοίξεις τον φάκελο του ενοικιαστή και πατήσεις την καρτέλα της μίσθωσης.
//
// Δηλαδή: καταχωρείς ενοικιαστή με ενοίκιο 500,00€, γυρίζεις στην Επισκόπηση,
// και η ταμειακή θέση λέει μηδέν. Το «Μπήκε το ενοίκιο» δεν εμφανίζεται ποτέ, η
// οφειλή δεν υπολογίζεται, η Πύλη ενοικιαστή είναι άδεια και το Ε2 δεν έχει τι
// να δηλώσει — μέχρι να πατήσει κανείς μια καρτέλα που δεν είχε λόγο να
// πατήσει. Ολόκληρη η αλυσίδα του προϊόντος κρεμόταν από μια περιήγηση.
//
// ── ΔΕΝ ΕΙΝΑΙ ΤΟ ΙΔΙΟ ΜΕ ΤΟ `syncTenantSchedule` ──────────────────────────
// Εκείνο γεμίζει ΗΜΕΡΟΛΟΓΙΟ και ΕΚΚΡΕΜΟΤΗΤΕΣ: υπενθυμίσεις, λήξη μίσθωσης,
// επιστροφή εγγύησης. Καμία γραμμή του δεν αγγίζει τα χρήματα. Εδώ γράφονται οι
// ΙΔΙΕΣ οι δόσεις, δηλαδή ο πίνακας από τον οποίο βγαίνει το Ε2. Τα δύο έμοιαζαν
// αρκετά ώστε το ένα να θεωρηθεί ότι κάνει και τη δουλειά του άλλου· δεν την
// έκανε και το κενό δεν φαινόταν πουθενά γιατί η απουσία δόσης δεν βγάζει
// σφάλμα, βγάζει μηδενικό.
//
// ── ΓΙΑΤΙ ΕΝΑ ΑΡΧΕΙΟ ΚΑΙ ΟΧΙ ΤΕΣΣΕΡΑ ─────────────────────────────────────
// Η μίσθωση γράφεται από τέσσερις διαδρομές: τη φόρμα ενοικιαστή, το παράθυρο
// μίσθωσης του φακέλου, τη σάρωση μισθωτηρίου και την ίδια την PaymentsView. Αν
// η δημιουργία γραφόταν σε καθεμία, τρεις από τις τέσσερις θα ξεχνιόνταν την
// επόμενη φορά.
//
// ── ΟΙ ΠΕΡΙΟΔΟΙ ΔΕΝ ΞΑΝΑΫΠΟΛΟΓΙΖΟΝΤΑΙ ΕΔΩ ────────────────────────────────
// Ποιοι μήνες γεννούν δόση το ξέρει ήδη το `expectedPeriods`, μαζί με το όριο
// της αποχώρησης και της λήξης. Μια δεύτερη γραφή του ίδιου κανόνα θα ήταν
// ακριβώς ο τρόπος με τον οποίο η οθόνη και η αποθήκευση θα κατέληγαν να
// φτιάχνουν διαφορετικές δόσεις για την ίδια μίσθωση.
//
// ── ΔΕΝ ΔΙΑΒΑΖΕΙ ΠΡΙΝ ΓΡΑΨΕΙ ──────────────────────────────────────────────
// Το κλειδί `UNIQUE(tenant_id, period_year, period_month)` κάνει τη γραφή
// ταυτοδύναμη: ζητούνται ΟΛΕΣ οι αναμενόμενες δόσεις με `ignoreDuplicates` και
// η βάση κρατά όσες λείπουν. Μια ήδη εισπραγμένη δόση ΔΕΝ αγγίζεται — αυτό δεν
// είναι λεπτομέρεια απόδοσης, είναι ο λόγος που η επαναποθήκευση της μίσθωσης
// δεν σβήνει πληρωμές.
// ═══════════════════════════════════════════════════════════════════════════

import type { SupabaseClient } from '@supabase/supabase-js';
import * as rentStore from '@/lib/data/rent';
import type { InstalmentPeriod } from '@/lib/rent/frequency';
import {
  expectedPeriods, tenantBaseRent, tenantServicesCharge, r2, type Tenant,
} from './TabTenantTypes';

/** Η ημέρα λήξης, κλεισμένη στο 1–28 ώστε ο Φεβρουάριος να υπάρχει πάντα. */
export const dueDayOf = (t: { rent_due_day?: number | null }): number =>
  Math.min(Math.max(1, t.rent_due_day || 1), 28);

/**
 * Οι γραμμές για συγκεκριμένες περιόδους, με ανάλυση ενοικίου και υπηρεσιών.
 *
 * ΤΟ ΠΟΣΟ ΑΚΟΛΟΥΘΕΙ ΤΗ ΣΥΧΝΟΤΗΤΑ. Μια τριμηνιαία δόση ΕΙΝΑΙ τρία μηνιαία
 * μισθώματα· αν άλλαζε μόνο το βήμα των ημερομηνιών, ο ιδιοκτήτης θα εισέπραττε
 * το ένα τρίτο του ετήσιου ενοικίου και το ίδιο θα δήλωνε στο Ε2.
 *
 * Δέχεται ΥΠΟΣΥΝΟΛΟ περιόδων γιατί η οθόνη των εισπράξεων γράφει μόνο όσες
 * λείπουν, ώστε να μπορεί να πει πόσες δημιούργησε.
 */
export function rowsForPeriods(
  periods: readonly InstalmentPeriod[], t: Tenant, propertyId: string, userId: string,
): rentStore.RentPatch[] {
  const base = tenantBaseRent(t);
  const services = tenantServicesCharge(t);
  return periods.map(p => ({
    tenant_id: t.id,
    property_id: propertyId,
    user_id: userId,
    period_year: p.year,
    period_month: p.month,
    amount: r2((base + services) * p.months),
    base_rent: r2(base * p.months),
    services_charge: r2(services * p.months),
    paid: false,
    due_date: p.due_date,
  }));
}

/** Ολες οι γραμμές που πρέπει να υπάρχουν για μια μίσθωση, από την έναρξη ώς σήμερα. */
export function instalmentRows(
  t: Tenant, propertyId: string, userId: string, dueDay = dueDayOf(t),
): rentStore.RentPatch[] {
  return rowsForPeriods(expectedPeriods(t, dueDay), t, propertyId, userId);
}

export interface SyncOutcome {
  /** Πόσες δόσεις ζητήθηκαν. Μηδέν σημαίνει «η μίσθωση δεν γεννά δόσεις ακόμη». */
  requested: number;
  /** Το μήνυμα του σφάλματος, ή κενό. */
  error: string;
}

/**
 * Φέρνει τις δόσεις μιας μίσθωσης σε συμφωνία με τα στοιχεία της.
 *
 * ΤΟ ΜΗΔΕΝ ΔΕΝ ΕΙΝΑΙ ΣΦΑΛΜΑ. Μίσθωση χωρίς έναρξη ή χωρίς ενοίκιο —
 * μισοσυμπληρωμένη καρτέλα, ή σάρωση που δεν βρήκε ποσό — απλώς δεν γεννά
 * δόσεις ακόμη. Το να «αποτύγχανε» εκεί θα σήμαινε κόκκινο μήνυμα σε κάθε
 * αποθήκευση προσχεδίου.
 */
export async function syncInstalments(
  db: SupabaseClient, t: Tenant, propertyId: string, userId: string,
): Promise<SyncOutcome> {
  const rows = instalmentRows(t, propertyId, userId);
  if (!rows.length) return { requested: 0, error: '' };
  const { error } = await rentStore.upsertPeriods(db, rows, { ignoreDuplicates: true });
  return { requested: rows.length, error: error?.message || '' };
}
