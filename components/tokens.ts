// ═══════════════════════════════════════════════════════════════════════════
// PROPERWISE — tokens.ts: τα σχεδιαστικά tokens, ΧΩΡΙΣ React
// ─────────────────────────────────────────────────────────────────────────
// ΓΙΑΤΙ ΞΕΧΩΡΙΣΤΟ ΑΡΧΕΙΟ ΚΑΙ ΟΧΙ ΜΕΣΑ ΣΤΟ Theme.tsx:
//
// Το Theme.tsx είναι `'use client'`. Όταν ένα Server Component εισάγει κάτι από
// module πελάτη, ο Next ΔΕΝ του δίνει την πραγματική τιμή — του δίνει αναφορά
// πελάτη. Το `T` έφτανε στον server ως `undefined` και το `T.font.sans` έριχνε
//     TypeError: Cannot read properties of undefined (reading 'sans')   ← το μήνυμα του περιηγητή
// στο SSR. Ολόκληρη η δημόσια σελίδα (αρχική, «Ποιοι είμαστε», Απόρρητο, Όροι,
// «χωρίς σύνδεση») έδειχνε το πλαίσιο σφάλματος αντί για περιεχόμενο.
//
// Δεν ήταν ορατό σε καμία δοκιμή: το `npm run build` περνά καθαρά, γιατί το
// σφάλμα συμβαίνει στην ΑΠΟΔΟΣΗ της σελίδας, όχι στη μεταγλώττιση. Γι' αυτό
// προστέθηκε και έλεγχος στο CI (scripts/check-server-imports.mjs) που κόβει
// κάθε νέα εισαγωγή token από module πελάτη μέσα σε Server Component.
//
// Εδώ μένει ό,τι είναι ΚΑΘΑΡΑ δεδομένα και συναρτήσεις — καμία εξάρτηση από
// React, άρα μπορεί να το εισάγει και ο server και ο πελάτης, με ασφάλεια.
// Το Theme.tsx τα ξανα-εξάγει, ώστε καμία υπάρχουσα εισαγωγή να μη χαλάσει.
// ═══════════════════════════════════════════════════════════════════════════

// ── Tokens (ίδια ονόματα με τα Bills, μηδενική αλλαγή νοοτροπίας) ─────────
export const T = {
  // ── Η ΚΛΙΜΑΚΑ ΓΩΝΙΑΣ, ΟΛΟΚΛΗΡΗ ────────────────────────────────────────────
  // ΤΑ ΟΝΟΜΑΤΑ ΗΤΑΝ ΤΕΣΣΕΡΑ ΚΑΙ ΟΙ ΤΙΜΕΣ ΔΕΚΑΤΡΕΙΣ. Μετρημένο στην πηγή: 444
  // γωνίες γραμμένες με ωμό αριθμό, σε 13 διαφορετικές τιμές, ενώ η κλίμακα
  // ονόμαζε μόνο 10, 14, 18 και 100. Οσο μια τιμή δεν έχει όνομα, δεν μπορεί να
  // ζητηθεί: ο επόμενος γράφει τον αριθμό που του φαίνεται σωστός και η ίδια
  // επιφάνεια βγαίνει 11 σε μια οθόνη, 12 σε άλλη και 14 σε τρίτη.
  //
  // Τα βήματα εδώ ΔΕΝ είναι εφεύρεση· είναι όσα ήδη χρησιμοποιεί η εφαρμογή,
  // με τα ονόματά τους: η γραμμή προόδου των 2, το σημαδάκι των 6, το πλακίδιο
  // των 8, το εσωτερικό κουτί και το κουμπί των 10, το αναδυόμενο των 12, η
  // κάρτα των 14, το παράθυρο των 18 και η πιλούλα.
  radius: { hair: 2, xs: 6, chip: 8, inner: 10, btn: 10, popup: 12, card: 14, modal: 18, badge: 100, pill: 100 },
  // Ύψη control — ΜΙΑ κλίμακα για κάθε κουμπί/πεδίο/chip, ώστε τίποτα να μη
  // «χοροπηδά» από οθόνη σε οθόνη: sm=chips, md=κανονικά πεδία, lg=κύρια πεδία.
  //
  // ΓΙΑΤΙ ΜΕΤΑΒΛΗΤΕΣ CSS ΚΑΙ ΟΧΙ ΑΡΙΘΜΟΙ. Η κλίμακα ήταν 32/36/40 — και τα τρία
  // κάτω από τα 44 εικονοστοιχεία που θέλει το δάχτυλο. Σε 215 σημεία γράφεται
  // ως inline style, όπου καμία media query δεν φτάνει: το `.btn { min-height:
  // 44px }` του globals.css δεν έσωζε τίποτα, γιατί η κλάση χρησιμοποιείται μία
  // φορά σε όλο το repo. Ως μεταβλητή, η ίδια γραμμή ανεβαίνει σε 44 όταν ο
  // δείκτης είναι δάχτυλο (globals.css, `@media (pointer: coarse)`) και μένει
  // σφιχτή στο ποντίκι. Μία αλλαγή, ένα σημείο, καμία διπλή κλίμακα.
  h: { sm: 'var(--h-sm)', md: 'var(--h-md)', lg: 'var(--h-lg)' },
  // Επικάλυψη παραθύρου — μία τιμή παντού (πριν υπήρχαν 7 διαφορετικές).
  scrim: 'rgba(0,0,0,0.55)',
  font: {
    // Γραμματοσειρές που φορτώνει self-hosted το app (globals.css): Inter + Roboto Mono.
    sans: "'Inter', system-ui, sans-serif",
    mono: "'Roboto Mono', 'JetBrains Mono', monospace",
    // Μεγάλοι αριθμοί «κεφαλίδας» (KPI): σφιχτή sans με tabular ψηφία, χωρίς τα
    // πλατιά κενά του monospace γύρω από κόμμα/τελεία. Το mono μένει για πυκνούς πίνακες.
    num:  "'Inter', system-ui, sans-serif",
  },
  sp: { xs: 4, sm: 8, md: 12, lg: 16, xl: 20, xxl: 24, section: 32 },
  // Καμπύλες κίνησης Google (Material 3), μία πηγή για όλα τα transitions.
  ease: { standard: 'cubic-bezier(0.2, 0, 0, 1)', emphasized: 'cubic-bezier(0.3, 0, 0, 1)', decel: 'cubic-bezier(0, 0, 0, 1)' },
} as const;

// ── Τυπογραφική κλίμακα, ΜΙΑ πηγή αλήθειας για μεγέθη/βάρη/spacing.
// Στόχος: «Google οπτική» ομοιομορφία, ίδιοι τίτλοι/ετικέτες/τιμές παντού.
// Χρήση: <div style={{ ...TT.label }}>…</div>  ή  style={TT.kpi}
// ═══ ΤΟ ΚΕΙΜΕΝΟ ΠΟΥ ΔΕΝ ΠΑΕΙ ΠΕΡΑ ΠΕΡΑ ═══════════════════════════════════
//
// Μια επικεφαλίδα δύο γραμμών που αφήνει μία λέξη μόνη της στη δεύτερη
// («…και από ποια ημερομηνία / μετράει.») δεν διαβάζεται ως τίτλος· διαβάζεται
// ως λάθος. Το ίδιο και μια παράγραφος που τελειώνει με ορφανή λέξη.
//
// Ο περιηγητής ξέρει να το λύσει μόνος του και η αρχική σελίδα το ζητούσε ήδη
// σε δεκατρία σημεία με `textWrap` γραμμένο στο χέρι. Ο πίνακας ελέγχου δεν το
// ζητούσε πουθενά — γι᾽ αυτό η αρχική έδειχνε δουλεμένη και οι οθόνες μέσα
// πρόχειρες. Δηλώνεται εδώ, στην κλίμακα, ώστε να ισχύει σε ΚΑΘΕ επικεφαλίδα
// και κάθε παράγραφο της εφαρμογής χωρίς να το θυμάται κανείς:
//
//   balance  για τίτλους — μοιράζει τις λέξεις σε ίσες γραμμές
//   pretty   για κείμενο — δεν αφήνει ποτέ μία λέξη μόνη της στο τέλος
//
// Είναι σταδιακή βελτίωση: όπου δεν υποστηρίζεται, το κείμενο αναδιπλώνεται
// όπως πριν. Καμία αλλαγή σε μέγεθος, βάρος ή χρώμα.
const BALANCE = { textWrap: 'balance' as const };
const PRETTY  = { textWrap: 'pretty'  as const };

export const TT = {
  display: { fontFamily: T.font.sans, fontSize: 28, fontWeight: 700, letterSpacing: '-0.02em', lineHeight: 1.15, color: 'var(--text-primary)', ...BALANCE },
  // Μικρότερο display για μεγάλους αριθμούς μέσα σε κάρτες (π.χ. ανταμοιβές).
  displaySm: { fontFamily: T.font.sans, fontSize: 24, fontWeight: 700, letterSpacing: '-0.02em', lineHeight: 1.15, color: 'var(--text-primary)', ...BALANCE },
  h1:      { fontFamily: T.font.sans, fontSize: 20, fontWeight: 700, letterSpacing: '-0.01em', lineHeight: 1.25, color: 'var(--text-primary)', ...BALANCE },
  h2:      { fontFamily: T.font.sans, fontSize: 16, fontWeight: 700, letterSpacing: '-0.01em', lineHeight: 1.3,  color: 'var(--text-primary)', ...BALANCE },
  // Ετικέτα ενότητας, η uppercase «τελεία» των Bills, τυποποιημένη.
  label:   { fontFamily: T.font.sans, fontSize: 'var(--fs-xs)', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase' as const, color: 'var(--text-secondary)', ...BALANCE },
  body:    { fontFamily: T.font.sans, fontSize: 'var(--fs-base)', fontWeight: 400, lineHeight: 1.55, color: 'var(--text-primary)', ...PRETTY },
  bodySm:  { fontFamily: T.font.sans, fontSize: 12, fontWeight: 400, lineHeight: 1.5,  color: 'var(--text-secondary)', ...PRETTY },
  caption: { fontFamily: T.font.sans, fontSize: 'var(--fs-xs)', fontWeight: 400, lineHeight: 1.45, color: 'var(--text-tertiary)', ...PRETTY },
  // Μεγάλοι αριθμοί KPI: σφιχτή sans (num) + tabular. Το πυκνό mono μένει για πίνακες.
  kpi:     { fontFamily: T.font.num, fontSize: 22, fontWeight: 700, fontVariantNumeric: 'tabular-nums' as const, letterSpacing: '-0.01em', lineHeight: 1, color: 'var(--text-primary)' },
  /**
   * ΓΙΑ ΚΩΔΙΚΟΥΣ, ΟΧΙ ΓΙΑ ΠΟΣΑ. Η μονοδιάστημη γραμματοσειρά υπάρχει για κείμενο
   * που διαβάζεται ΧΑΡΑΚΤΗΡΑ ΧΑΡΑΚΤΗΡΑ, όπως ένας κωδικός ανάκτησης: εκεί η
   * διάκριση του «l» από το «1» αξίζει το τίμημα.
   */
  mono:    { fontFamily: T.font.mono, fontSize: 'var(--fs-base)', fontWeight: 500, fontVariantNumeric: 'tabular-nums' as const, color: 'var(--text-primary)' },
  /**
   * ΕΝΑΣ ΑΡΙΘΜΟΣ ΜΕΣΑ ΣΕ ΓΡΑΜΜΗ, ΣΤΗΝ ΑΡΙΘΜΗΤΙΚΗ ΤΟΥ ΘΕΜΑΤΟΣ.
   *
   * Τέσσερα σημεία έγραφαν ποσά και ποσοστά με το `mono`, δηλαδή σε ΑΛΛΗ
   * γραμματοσειρά από κάθε άλλο ποσό της ίδιας οθόνης: το σύνολο μήνα στις
   * Δαπάνες, δύο γραμμές του ΕΝΦΙΑ και μία του Σχεδίου. Το «1.152,00 €» δίπλα
   * στο «60,00 €» φαινόταν άλλο είδος πράγματος, σε απόσταση μιας ματιάς.
   *
   * Η στοίχιση δεν χάνεται: το `tabular-nums` δίνει ίσο πλάτος στα ψηφία, που
   * είναι ο πραγματικός λόγος που ήθελε κανείς μονοδιάστημη σε στήλη ποσών.
   */
  figure:  { fontFamily: T.font.num, fontSize: 'var(--fs-base)', fontWeight: 500, fontVariantNumeric: 'tabular-nums' as const, color: 'var(--text-primary)' },
} as const;

// ── Κελί ιστορικού λογαριασμών ─────────────────────────────────────────────
// Το πεδίο του δωδεκάμηνου ιστορικού (Ρεύμα, Κοινόχρηστα, Υπηρεσίες) ήταν
// γραμμένο τρεις φορές και το ένα αντίγραφο είχε ΧΑΣΕΙ το `cursor: 'pointer'`:
// στην ίδια γραμμή πεδίων, τα δύο έδειχναν χεράκι και το τρίτο όχι, χωρίς
// καμία διαφορά στη συμπεριφορά. Εδώ ζει μία φορά.
export const histInputStyle = (isCurrent: boolean, isHovered = false) => ({
  width: '100%',
  background: isCurrent ? 'var(--accent-soft)' : isHovered ? 'var(--bg-elevated)' : 'var(--bg-base)',
  border: `1px solid ${isCurrent ? 'var(--accent)' : isHovered ? 'var(--border-default)' : 'var(--border-subtle)'}`,
  borderRadius: T.radius.badge,
  padding: '6px 4px',
  color: 'var(--text-primary)',
  fontSize: 'var(--fs-xs)',
  fontFamily: T.font.mono,
  fontVariantNumeric: 'tabular-nums' as const,
  outline: 'none',
  textAlign: 'center' as const,
  boxSizing: 'border-box' as const,
  transition: 'background-color 0.15s, border-color 0.15s, color 0.15s, box-shadow 0.15s, transform 0.15s, opacity 0.15s',
  cursor: 'pointer' as const,
});

// ── ΑΡΙΘΜΟΙ ────────────────────────────────────────────────────────────────
// Οι τύποι ζουν στο `lib/core/format.ts` και επανεξάγονται εδώ. Ο λόγος: τους
// χρειάζεται και ο πυρήνας (insights, δάνεια, αποδόσεις παράγουν κείμενο για
// τον χρήστη), αλλά το `lib/` δεν επιτρέπεται να εισάγει από το `components/`.
// Οι οθόνες συνεχίζουν να γράφουν `import { fe } from '@/components/Theme'`.
export { fe, feAuto, feRate, feCompact, fp, fn, feOr, fpOr, DASH, isBlankMetric, ABSENT, ABSENT_DATE, ABSENT_SHORT, grUpper } from '@/lib/core/format';

// ── ΜΙΑ ΗΜΕΡΟΛΟΓΙΑΚΗ ΗΜΕΡΑ ΔΕΝ ΕΙΝΑΙ ΣΤΙΓΜΗ ──────────────────────────────
//
// ΤΟ ΣΦΑΛΜΑ, ΜΕΤΡΗΜΕΝΟ. Εδώ γραφόταν σκέτο `new Date(d)`. Για συμβολοσειρά
// «2026-01-01» αυτό είναι μεσάνυχτα UTC, ενώ το `toLocaleDateString`
// μορφοποιεί σε ΤΟΠΙΚΗ ώρα. Σε ζώνη με αρνητική απόκλιση το αποτέλεσμα είναι
// η ΠΡΟΗΓΟΥΜΕΝΗ ημέρα:
//
//     Αθήνα     →  1 Ιανουαρίου
//     Νέα Υόρκη →  31 Δεκεμβρίου
//
// Οι δύο αυτές συναρτήσεις τυπώνουν κάθε ημερομηνία της εφαρμογής — λήξη
// μισθωτηρίου, ημέρα άφιξης στο μήνυμα προς τον επισκέπτη, ημερομηνία
// απόδειξης. Ο κώδικας τρέχει στον περιηγητή ΤΟΥ ΧΡΗΣΤΗ, όχι στον διακομιστή.
//
// Η ΛΥΣΗ ΕΙΝΑΙ ΤΟ `T00:00:00`: κάνει την κατασκευή ΤΟΠΙΚΗ, οπότε συμφωνεί με
// τον τοπικό μορφοποιητή και η ημέρα μένει αυτή που γράφτηκε. Ένα αντικείμενο
// `Date` περνά αυτούσιο — εκεί ο καλών έχει ήδη αποφασίσει τι στιγμή εννοεί.
// Η `localDay` ζει στο `lib/core/time.ts`, μαζί με τα `isoYear`/`isoMonth` που
// κλείνουν το ΙΔΙΟ σφάλμα: δεν την χρειάζονται μόνο οι δύο μορφοποιητές από
// κάτω, αλλά και τα εκτυπώσιμα έγγραφα — και το `lib/` δεν επιτρέπεται να
// εισάγει από το `components/`. Επανεξάγεται εδώ γιατί κάθε οθόνη που τυπώνει
// ημερολογιακή ημέρα με δικές της επιλογές έχει το ίδιο πρόβλημα.
import { localDay } from '@/lib/core/time';
export { localDay };

export const fd = (d: string | Date) =>
  localDay(d).toLocaleDateString('el-GR', { day: '2-digit', month: 'short', year: 'numeric' });

// Πλήρης μορφή χωρίς συντομογραφία μήνα («30 Ιουνίου 2026»).
export const fdLong = (d: string | Date) =>
  localDay(d).toLocaleDateString('el-GR', { day: 'numeric', month: 'long', year: 'numeric' });

// ── Σημασιολογικοί τόνοι, ρόλοι, όχι αυθαίρετα χρώματα ─────────────────────
export type Tone = 'accent' | 'info' | 'positive' | 'warning' | 'negative' | 'neutral';

// ═══ ΤΟ ΠΕΔΙΟ ΠΟΣΟΥ ΠΟΥ ΑΠΛΩΝΟΤΑΝ ΣΕ 750 ΕΙΚΟΝΟΣΤΟΙΧΕΙΑ ═══════════════════
//
// Οι φόρμες έγραφαν `repeat(auto-fit, minmax(200px, 1fr))`. Το `auto-fit`
// ΤΕΝΤΩΝΕΙ τις στήλες ώστε να γεμίσει το πλάτος: δύο πεδία σε οθόνη 1.500
// εικονοστοιχείων γίνονταν δύο κουτιά των 750, το καθένα για να δεχτεί ένα
// ποσό τεσσάρων ψηφίων. Το αποτέλεσμα δεν είναι απλώς άσχημο, είναι
// παραπλανητικό: το μέγεθος του πεδίου υπόσχεται περιεχόμενο που δεν υπάρχει,
// και η φόρμα διαβάζεται ασύμμετρη και φθηνή δίπλα στην ετικέτα της.
//
// Ένα πεδίο ποσού ή ημερομηνίας έχει ΦΥΣΙΚΟ πλάτος. Το `auto-fill` με ΑΝΩ όριο
// στην στήλη το κρατά: τα πεδία στοιχίζονται από αριστερά, κρατούν σταθερό
// μέγεθος και ο επιπλέον χώρος μένει χώρος αντί να μοιραστεί σε κουτιά.
//
// ΠΟΤΕ ΔΕΝ ΧΡΗΣΙΜΟΠΟΙΕΙΤΑΙ. Για πλέγματα ΠΛΑΚΙΔΙΩΝ (μετρικές, κάρτες, επιλογές)
// το τέντωμα είναι σωστό: εκεί το πλακίδιο ΘΕΛΕΙ να πιάσει τον χώρο του.
// ── ΤΟ ΠΛΕΓΜΑ ΤΩΝ ΠΛΑΚΙΔΙΩΝ ────────────────────────────────────────────────
// Το αντίθετο του `formGrid` και για τον αντίθετο λόγο. Ένα πλακίδιο επιλογής
// (υπηρεσία, κανάλι, μετρική) ΘΕΛΕΙ να πιάσει τον χώρο του: όταν κάθε πλακίδιο
// έχει το πλάτος του ονόματός του, μια σειρά με «Netflix» και «Amazon Prime
// Video» δεν στοιχίζεται πουθενά και η τελευταία σειρά μένει μισή, με τρύπα
// δεξιά. Το μάτι δεν βρίσκει στήλη να ακολουθήσει.
//
// Με ίσες στήλες, τα ονόματα ξεκινούν στο ίδιο σημείο και τα ποσά τελειώνουν
// στο ίδιο: δύο κάθετες γραμμές αντί για δεκαοκτώ ελεύθερες άκρες. Η μισή
// τελευταία σειρά εξακολουθεί να είναι μισή — αλλά διαβάζεται ως πλέγμα που
// συνεχίζει, όχι ως γραμμή που έσπασε.
//
// Το `min(100%, …)` κρατά το πλακίδιο μέσα στο πλαίσιο σε στενή οθόνη· χωρίς
// αυτό, ένα ελάχιστο 190 σε οθόνη 360 βγάζει οριζόντια κύλιση.
/**
 * Σειρά πλακιδίων ΙΣΟΥ πλάτους, κεντραρισμένη.
 *
 * Η διαφορά από το `tileGrid` είναι η μισή τελευταία σειρά. Το πλέγμα την αφήνει
 * κολλημένη αριστερά, με τρύπα δεξιά· η κεντραρισμένη σειρά τη βάζει στη μέση,
 * και το μπλοκ διαβάζεται συμμετρικό αντί για κομμένο.
 *
 * ΤΟ ΠΛΑΤΟΣ ΜΕΝΕΙ ΣΤΑΘΕΡΟ (`0 0 <width>`), γιατί αυτό ήταν εξαρχής το πρόβλημα:
 * με ελεύθερο πλάτος, το «Netflix» βγαίνει μισό από το «Amazon Prime Video» και
 * καμία άκρη δεν στοιχίζεται με καμία. Με `flex-grow` θα ήταν χειρότερα: η
 * τελευταία σειρά, έχοντας λιγότερα πλακίδια, θα τα φούσκωνε πλατύτερα από τα
 * από πάνω τους.
 *
 * Το `maxWidth: '100%'` στο πλακίδιο το κρατά μέσα στο πλαίσιο σε στενή οθόνη.
 */
export const tileRow = (width = 210, gap = 8) => ({
  display: 'flex' as const,
  flexWrap: 'wrap' as const,
  justifyContent: 'center' as const,
  gap,
  // Το `min(100%, …)` κάνει το πλακίδιο πλήρους πλάτους σε στενή οθόνη: εκεί μια
  // στήλη των 210 μέσα σε κάρτα των 230 θα έμοιαζε χαμένη δίπλα στα πεδία από
  // κάτω, που πιάνουν όλο το πλάτος.
  '--tile-w': `min(100%, ${width}px)`,
});

export const tileGrid = (min = 190, gap = 8) => ({
  display: 'grid' as const,
  gridTemplateColumns: `repeat(auto-fit, minmax(min(100%, ${min}px), 1fr))`,
  gap,
});

export const formGrid = (min = 200, max = 280) => ({
  display: 'grid' as const,
  gridTemplateColumns: `repeat(auto-fill, minmax(min(100%, ${min}px), ${max}px))`,
  gap: 14,
});

/**
 * ΜΙΑ ΣΕΙΡΑ ΠΕΔΙΩΝ, ΙΣΑ ΜΟΙΡΑΣΜΕΝΗ ΣΕ ΟΛΟ ΤΟ ΠΛΑΤΟΣ.
 *
 * ΤΟ ΠΡΟΒΛΗΜΑ ΠΟΥ ΤΗ ΓΕΝΝΗΣΕ: το `formGrid` γεμίζει με `auto-fill` και ΚΟΒΕΙ
 * κάθε στήλη σε σταθερό μέγιστο. Σε μια κάρτα 1.350 pixel με τέσσερα πεδία των
 * 230, οι τρεις πρώτες στήλες πιάνουν 690 pixel, η τέταρτη δεν χωράει στο
 * υπόλοιπο που η ίδια η ρύθμιση έχει ήδη δεσμεύσει για κενές στήλες — και
 * πέφτει μόνη της από κάτω. Τρία πάνω, ένα κάτω, με μισή κάρτα άδεια δεξιά.
 * Δεν είναι σχεδιαστική επιλογή· είναι το τι απέμεινε.
 *
 * ΕΔΩ: `auto-fit` και `1fr`. Όσα πεδία χωρούν με το ελάχιστό τους μπαίνουν στην
 * ΙΔΙΑ γραμμή και μοιράζονται ΙΣΑ ό,τι περισσεύει — καμία στήλη πλατύτερη από
 * την άλλη, καμία τρύπα δεξιά. Όταν πραγματικά δεν χωρούν, τυλίγονται· και
 * τότε πάλι ίσα, γιατί το `auto-fit` μαζεύει τις κενές στήλες.
 *
 * ΤΟ ΕΛΑΧΙΣΤΟ ΕΙΝΑΙ Η ΜΟΝΗ ΡΥΘΜΙΣΗ — ΚΑΙ ΟΡΙΖΕΙ ΤΟ ΠΛΗΘΟΣ ΤΩΝ ΣΤΗΛΩΝ.
 *
 * Αυτό είναι το σημείο που ξεγελά. Όσο τα πεδία είναι ΛΙΓΟΤΕΡΑ από τις στήλες
 * που χωρούν, το `auto-fit` μαζεύει τις κενές και τα απλώνει ίσα σε μία σειρά:
 * τέσσερα πεδία με ελάχιστο 180 βγαίνουν τέσσερα, ό,τι πλάτος κι αν έχει η
 * κάρτα. Μόλις όμως τα πεδία ΞΕΠΕΡΑΣΟΥΝ τις στήλες που χωρούν, γεμίζει τη
 * σειρά και ρίχνει το υπόλοιπο κάτω — οκτώ πεδία με ελάχιστο 200 σε κάρτα
 * 1.400 δίνουν επτά και ένα, δηλαδή ακριβώς το πρόβλημα που ήρθε να λύσει.
 *
 * Άρα: για ΛΙΓΑ πεδία διάλεξε το ελάχιστο από την αναγνωσιμότητα. Για ΠΟΛΛΑ,
 * διάλεξέ το ώστε να βγει το πλήθος στηλών που θέλεις. Σε κάρτα 1.290 ως 1.450
 * εικονοστοιχείων, με κενό 12:
 *
 *     ελάχιστο 240 → πέντε στήλες      ελάχιστο 300 → τέσσερις
 *     ελάχιστο 360 → τρεις             ελάχιστο 500 → δύο
 *
 * Και σε στενότερη οθόνη πέφτουν μόνες τους, χωρίς media query.
 */
/**
 * ΡΗΤΟ ΠΛΗΘΟΣ ΣΤΗΛΩΝ, ΟΤΑΝ Η ΔΙΑΤΑΞΗ ΕΙΝΑΙ ΑΠΟΦΑΣΗ ΚΑΙ ΟΧΙ ΑΠΟΤΕΛΕΣΜΑ.
 *
 * Το `fieldRow` (auto-fit) δίνει όσες στήλες ΧΩΡΑΝΕ. Αυτό είναι σωστό όταν η
 * σειρά απλώς πρέπει να γεμίσει το πλάτος — αλλά ΟΧΙ όταν το πλήθος έχει
 * νόημα: οκτώ πεδία θέλουν τέσσερα και τέσσερα, δέκα πλακίδια πέντε και πέντε.
 *
 * ΚΑΙ ΥΠΑΡΧΕΙ ΛΟΓΟΣ ΠΟΥ ΤΟ auto-fit ΔΕΝ ΑΡΚΕΙ: το zoom του περιηγητή αλλάζει το
 * πλάτος σε εικονοστοιχεία CSS. Το ίδιο ελάχιστο βγάζει τέσσερις στήλες στο
 * 100% και τρεις στο 125% — δηλαδή η ίδια οθόνη έχει άλλη διάταξη σε κάθε
 * ρύθμιση και «τέσσερα και τέσσερα» γίνεται «τρία, τρία και δύο».
 *
 * Το πλήθος γράφεται εδώ και σπάει σε τρεις, δύο, μία στήλες μόνο σε πραγματικά
 * στενή οθόνη (βλ. `.fixed-cols` στο globals.css). Χρήση:
 *
 *     <div {...fixedCols(4)}>…</div>
 *
 * Η στοίχιση είναι στο ΚΑΤΩ άκρο, ώστε μια ετικέτα δύο γραμμών να μη σπρώχνει
 * το κουτί της πιο χαμηλά από τα διπλανά. Όταν όμως ένα κελί κουβαλά σημείωση
 * ΑΠΟ ΚΑΤΩ, το κάτω άκρο είναι η σημείωση και το κουτί ανεβαίνει: εκεί περνάς
 * `'start'`.
 */
export const fixedCols = (
  n: number, gap = 12, align: 'end' | 'start' | 'stretch' = 'end', cls = '',
  /**
   * ΠΟΣΕΣ ΣΤΗΛΕΣ ΣΤΗ ΜΕΣΑΙΑ ΟΘΟΝΗ, ΟΤΑΝ Ο ΔΙΑΙΡΕΤΗΣ ΔΕΝ ΑΡΚΕΙ.
   *
   * ΜΕΤΡΗΜΕΝΟ ΣΤΑ 768, ΣΤΗΝ «Επωνυμία στις αναφορές»: τρία πεδία, τρεις στήλες
   * των 215, δηλαδή κουτί 187 για τιμή που ζητά 194. Το βήμα λέει «τρία», γιατί
   * το τρία διαιρείται με το τρία — σωστός κανόνας για πλακίδια, λάθος για
   * πεδία που κρατούν επωνυμία και ηλεκτρονικό ταχυδρομείο. Οποιος ξέρει ότι τα
   * πεδία του θέλουν πλάτος το δηλώνει εδώ, αντί να το ανακαλύψει ο χρήστης.
   */
  md?: number,
) => ({
  className: cls ? `fixed-cols ${cls}` : 'fixed-cols',
  // Οι μεταβλητές CSS δεν υπάρχουν στον τύπο του React· ο ισχυρισμός γίνεται
  // εδώ, μία φορά, αντί σε κάθε κλήση.
  //
  // ΤΑ ΣΤΕΝΑ ΣΠΑΣΙΜΑΤΑ ΜΕΙΩΝΟΥΝ, ΔΕΝ ΟΡΙΖΟΥΝ. Γραμμένα ως σταθερό «τρεις» και
  // «δύο», μια σειρά ΔΥΟ στηλών γινόταν ΤΡΕΙΣ κάτω από τα 1.080 — δηλαδή το
  // στένεμα της οθόνης πρόσθετε στήλη. Το ελάχιστο κρατά το σπάσιμο σπάσιμο.
  style: {
    '--fc': n, '--fc-md': md ?? fcStep(n, 3), '--fc-sm': fcStep(n, 2),
    '--fc-gap': `${gap}px`, alignItems: align,
  } as Record<string, string | number>,
});

/**
 * Πόσες στήλες σε στενότερη οθόνη, ώστε να ΜΗΝ ΜΕΙΝΕΙ ΕΝΑ ΠΛΑΚΙΔΙΟ ΜΟΝΟ ΤΟΥ.
 *
 * ΤΟ ΣΦΑΛΜΑ, ΠΙΑΣΜΕΝΟ ΣΕ ΤΑΜΠΛΕΤΑ ΑΠΟ ΤΟΝ ΧΡΗΣΤΗ. Το βήμα ήταν `min(n, 3)`:
 * τέσσερα πλακίδια γίνονταν ΤΡΙΑ ΣΤΗΛΕΣ, δηλαδή 3+1, με το τέταρτο μόνο του και
 * τρύπα δεξιά του. Στον υπολογιστή οι στήλες είναι τέσσερις και δεν φαίνεται·
 * σε ταμπλέτα φαίνεται σε κάθε οθόνη που έχει τέσσερις δείκτες. Ο χρήστης το
 * ανέφερε τέσσερις φορές, με τη λέξη «ασύμμετρα».
 *
 * Ο ΚΑΝΟΝΑΣ: διάλεξε τον ΜΕΓΑΛΥΤΕΡΟ διαιρέτη του πλήθους που χωράει. Τέσσερα σε
 * δύο στήλες δίνει 2+2, έξι σε τρεις δίνει 3+3, οκτώ σε δύο δίνει τέσσερις
 * γεμάτες σειρές. Οταν δεν υπάρχει διαιρέτης (πέντε, επτά), ισχύει το παλιό
 * ταβάνι: πέντε σε τρεις δίνει 3+2, που ΔΕΝ είναι ορφανό.
 */
const fcStep = (n: number, cap: number): number => {
  for (let d = Math.min(n, cap); d >= 2; d--) if (n % d === 0) return d;
  // ΣΤΟ ΚΙΝΗΤΟ Η ΜΙΑ ΣΤΗΛΗ ΕΙΝΑΙ ΚΑΛΥΤΕΡΗ ΑΠΟ ΤΟ ΟΡΦΑΝΟ. Με ταβάνι δύο, ένα
  // πλακίδιο που μένει μόνο του πιάνει το μισό πλάτος και δίπλα του χάσκει
  // τρύπα ίσου μεγέθους: η ασυμμετρία είναι όσο μεγάλη γίνεται. Τρία πλακίδια
  // το ένα κάτω από το άλλο δεν είναι όμορφα, αλλά είναι ζυγισμένα.
  // Στο ταβάνι των τριών δεν ισχύει: το 3+2 διαβάζεται μια χαρά.
  return cap === 2 ? 1 : Math.min(n, cap);
};

/**
 * Μια σειρά πεδίων φόρμας, με ΓΕΜΑΤΗ τελευταία σειρά σε κάθε πλάτος.
 *
 * ΤΟ ΣΦΑΛΜΑ, ΠΙΑΣΜΕΝΟ ΣΕ ΤΑΜΠΛΕΤΑ. Ηταν πλέγμα με `auto-fit`, που δίνει όσες
 * στήλες ΧΩΡΑΝΕ και αφήνει την τελευταία σειρά μισή: πέντε πεδία τιμολόγησης
 * στα 820 έβγαιναν 4+1, με την «Ελάχιστη διαμονή» μόνη της και τρύπα δεξιά της
 * όσο τρία πεδία. Ο χρήστης το ανέφερε ως «ασύμμετρα, αζύγιστα».
 *
 * ΤΟ FLEX ΤΟ ΛΥΝΕΙ ΓΕΝΙΚΑ, ΓΙΑ ΚΑΘΕ ΠΛΗΘΟΣ ΚΑΙ ΚΑΘΕ ΠΛΑΤΟΣ: με `flex-grow: 1`
 * τα πεδία της τελευταίας σειράς ΑΠΛΩΝΟΝΤΑΙ και γεμίζουν τον χώρο. Δεν μένει
 * ποτέ τρύπα, όσα πεδία κι αν είναι. Ενα πεδίο που πιάνει όλο το πλάτος
 * διαβάζεται ως απόφαση· ένα πεδίο δίπλα σε τρύπα διαβάζεται ως σφάλμα.
 *
 * ΓΥΡΙΖΕΙ ΠΛΕΟΝ ΙΔΙΟΤΗΤΕΣ, ΟΧΙ ΣΤΥΛ, όπως το `fixedCols`: το γέμισμα της
 * τελευταίας σειράς θέλει κανόνα για τα ΠΑΙΔΙΑ και ένα ενσώματο στυλ δεν φτάνει
 * στα παιδιά του. Το `extra` κρατά τα σημεία κλήσης καθαρά.
 */
export const fieldRow = (min = 170, gap = 14, extra?: Record<string, string | number>, cls = '') => ({
  className: cls ? `field-row ${cls}` : 'field-row',
  style: {
    '--fr-min': `min(100%, ${min}px)`,
    '--fr-gap': `${gap}px`,
    ...extra,
  } as Record<string, string | number>,
});

// ═══════════════════════════════════════════════════════════════════════════
// ΟΙ ΣΤΑΘΜΕΣ ΤΗΣ ΣΤΟΙΒΑΞΗΣ, ΟΝΟΜΑΣΜΕΝΕΣ
// ─────────────────────────────────────────────────────────────────────────
// ΔΕΝ ΕΙΝΑΙ ΚΑΙΝΟΥΡΙΑ ΚΛΙΜΑΚΑ, ΕΙΝΑΙ Η ΥΠΑΡΧΟΥΣΑ ΓΡΑΜΜΕΝΗ. Μετρημένα στον
// κώδικα: τα παράθυρα ξεκινούν στο 1000 και ανεβαίνουν ανά δέκα με το βάθος
// (`OVERLAY_BASE_Z`), τα αναδυόμενα μενού και τα ημερολόγια κάθονται στο 2000,
// οι υποδείξεις `role="tooltip"` και το καλωσόρισμα στο 3000.
//
// ΤΟ ΣΦΑΛΜΑ ΠΟΥ ΤΙΣ ΕΦΕΡΕ ΕΔΩ. Η ειδοποίηση καθόταν ΚΙ ΑΥΤΗ στο 2000, δηλαδή
// στην ίδια στάθμη με τα μενού. Οταν δύο αδέλφια δηλώνουν το ίδιο z-index,
// ποιο ζωγραφίζεται πάνω το κρίνει η σειρά στο DOM και όχι η πρόθεση: ένα
// «δεν αποθηκεύτηκε» μπορούσε να βγει ΠΙΣΩ από ανοιχτό επιλογέα. Σε τηλέφωνο
// είναι χειρότερο, γιατί το μενού πιάνει σχεδόν όλο το πλάτος και η
// ειδοποίηση στέκει κεντραρισμένη στον πάτο, ακριβώς από κάτω του.
//
// Η ΕΙΔΟΠΟΙΗΣΗ ΠΑΕΙ ΑΝΑΜΕΣΑ ΣΕ ΜΕΝΟΥ ΚΑΙ ΥΠΟΔΕΙΞΗ. Πάνω από ό,τι χειρίζεται ο
// χρήστης, γιατί είναι μήνυμα του συστήματος και πρέπει να διαβαστεί. Κάτω από
// την υπόδειξη, που εμφανίζεται μόνο όσο ο χρήστης κρατά ένα συγκεκριμένο
// στοιχείο και σβήνει μόλις το αφήσει.
//
// ΟΙ ΥΠΟΛΟΙΠΕΣ 55 ΩΜΕΣ ΣΤΑΘΜΕΣ ΔΕΝ ΜΕΤΑΚΟΜΙΖΟΥΝ ΕΔΩ ΣΗΜΕΡΑ. Η σειρά στοίβαξης
// δεν αποδεικνύεται με ανάγνωση: αποδεικνύεται βλέποντας κάθε παράθυρο
// ανοιχτό. Ο φύλακας `guard-z-layers` κρατά τον αριθμό να μη μεγαλώσει ώσπου
// να γίνει αυτό. Εδώ μπαίνει η σκάλα, όχι η μετακόμιση.
// ═══════════════════════════════════════════════════════════════════════════
export const Z = {
  /** Παράθυρα και συρτάρια. Ανεβαίνει ανά δέκα με το βάθος. */
  overlay: 1000,
  /** Μενού επιλογέα, ημερολόγιο, αναδυόμενη λίστα. */
  popover: 2000,
  /** Μήνυμα συστήματος: πάνω από ό,τι χειρίζεται ο χρήστης. */
  toast: 2500,
  /** Υπόδειξη που ζει όσο την κρατά ο χρήστης· εκεί και το καλωσόρισμα. */
  tooltip: 3000,
} as const;
