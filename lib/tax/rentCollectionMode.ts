// ═══════════════════════════════════════════════════════════════════════════
// ΤΡΑΠΕΖΙΚΗ ΕΙΣΠΡΑΞΗ: Η ΑΠΑΝΤΗΣΗ ΥΠΑΡΧΕΙ, ΔΕΝ ΜΑΝΤΕΥΕΤΑΙ
// ─────────────────────────────────────────────────────────────────────────
// ΤΙ ΙΣΧΥΕ. Η Λογιστική ρωτούσε «τα ενοίκια εισπράττονται μέσω τραπέζης;» με
// `useState(true)`. Δηλαδή ΠΡΟΕΠΙΛΕΓΕ την εκδοχή που δίνει την τεκμαρτή
// έκπτωση 5% — τον ΜΙΚΡΟΤΕΡΟ φόρο — για ερώτημα που η εφαρμογή ήδη γνωρίζει.
//
// Και το γνωρίζει σε ΔΥΟ επίπεδα:
//
//   ΑΠΟΔΕΙΞΗ    `rent_payments.method` ανά είσπραξη («Μετρητά», «Τραπεζική
//               κατάθεση», «Ηλεκτρονική πληρωμή», «Κάρτα») — τι ΕΓΙΝΕ.
//   ΠΡΟΘΕΣΗ     `tenants.e_payment`, διακόπτης που όρισε ο ίδιος ο ιδιοκτήτης
//               στη μίσθωση — τι ΣΥΜΦΩΝΗΘΗΚΕ.
//
// Η καρτέλα «Φροντίδα μισθωτή» χρησιμοποιούσε ήδη τη δεύτερη σωστά. Η
// Λογιστική — η οθόνη που παράγει τον φόρο ΚΑΙ τον φάκελο του λογιστή — τις
// αγνοούσε και τις δύο.
//
// ΓΙΑΤΙ Η ΠΡΟΕΠΙΛΟΓΗ ΗΤΑΝ ΤΟ ΠΡΟΒΛΗΜΑ, ΚΑΙ ΟΧΙ Η ΕΡΩΤΗΣΗ. Το «ναι» δεν είναι
// ουδέτερη προεπιλογή: είναι η κερδοφόρα. Ενας ιδιοκτήτης που εισπράττει σε
// μετρητά και δεν πρόσεξε το κουτάκι βλέπει φόρο υπολογισμένο στο 95% και τον
// παραδίδει στον λογιστή του. Το λάθος δεν κραυγάζει — απλώς είναι μικρότερο
// από την αλήθεια, που είναι ακριβώς ο τρόπος να μη γίνει αντιληπτό.
//
// Ο ΚΑΝΟΝΑΣ: Η ΑΠΟΔΕΙΞΗ ΝΙΚΑ ΤΗΝ ΠΡΟΘΕΣΗ, ΚΑΙ ΜΙΑ ΕΙΣΠΡΑΞΗ ΣΕ ΜΕΤΡΗΤΑ ΑΡΚΕΙ.
// Ο νόμος (ν.5246/2025) δεν δίνει την έκπτωση κατ' αναλογία: ζητά είσπραξη με
// τραπεζικό ή ηλεκτρονικό μέσο. Μία καταγεγραμμένη είσπραξη σε μετρητά μέσα
// στη χρήση θέτει την έκπτωση σε κίνδυνο και η οθόνη οφείλει να το δείξει
// αντί να το στρογγυλέψει.
// ═══════════════════════════════════════════════════════════════════════════

/** Οι τρόποι που ΔΕΝ είναι τραπεζικό ή ηλεκτρονικό μέσο. */
const CASH = 'Μετρητά';

export interface RentPaidRow {
  paid?: boolean | null;
  period_year?: number | null;
  method?: string | null;
}

export type CollectionBasis = 'payments' | 'lease' | 'unknown';

export interface RentCollectionMode {
  /** Ισχύει η προϋπόθεση τραπεζικής είσπραξης για την τεκμαρτή έκπτωση; */
  viaBank: boolean;
  /** Από πού βγήκε η απάντηση. */
  basis: CollectionBasis;
  /** Πόσες εισπράξεις της χρήσης έχουν καταγεγραμμένο τρόπο. */
  withMethod: number;
  /** Πόσες από αυτές είναι μετρητά. */
  cash: number;
}

/**
 * Ο τρόπος είσπραξης της χρήσης, από ό,τι ξέρει η εφαρμογή.
 *
 * `leaseViaBank`: το `tenants.e_payment` της τρέχουσας μίσθωσης (ή `null` όταν
 * δεν υπάρχει μίσθωση). Χρησιμοποιείται ΜΟΝΟ όταν δεν υπάρχει καμία είσπραξη
 * με καταγεγραμμένο τρόπο — η πρόθεση δεν υπερισχύει της απόδειξης.
 */
export function rentCollectionMode(
  rows: readonly RentPaidRow[],
  year: number,
  leaseViaBank: boolean | null,
): RentCollectionMode {
  const ofYear = rows.filter(r => r.paid === true && r.period_year === year);
  const withMethod = ofYear.filter(r => !!(r.method || '').trim());
  const cash = withMethod.filter(r => (r.method || '').trim() === CASH);

  if (withMethod.length > 0) {
    return { viaBank: cash.length === 0, basis: 'payments', withMethod: withMethod.length, cash: cash.length };
  }
  if (leaseViaBank !== null) {
    return { viaBank: leaseViaBank, basis: 'lease', withMethod: 0, cash: 0 };
  }
  // Καμία απόδειξη και καμία μίσθωση. ΔΕΝ επιλέγουμε την κερδοφόρα εκδοχή:
  // χωρίς τεκμηρίωση δεν υπάρχει δικαίωμα στην έκπτωση και η οθόνη το λέει.
  return { viaBank: false, basis: 'unknown', withMethod: 0, cash: 0 };
}

/** Η μία διατύπωση της αιτιολόγησης, ίδια σε κάθε οθόνη. */
export function collectionModeReason(m: RentCollectionMode): string {
  if (m.basis === 'payments') {
    return m.cash > 0
      ? `${m.cash} από ${m.withMethod} καταχωρημένες εισπράξεις της χρήσης είναι σε μετρητά.`
      : `Και οι ${m.withMethod} καταχωρημένες εισπράξεις της χρήσης είναι με τραπεζικό ή ηλεκτρονικό μέσο.`;
  }
  if (m.basis === 'lease') {
    return m.viaBank
      ? 'Από τη μίσθωση: έχει δηλωθεί είσπραξη μέσω τραπέζης. Καμία είσπραξη της χρήσης δεν έχει καταχωρημένο τρόπο.'
      : 'Από τη μίσθωση: δεν έχει δηλωθεί είσπραξη μέσω τραπέζης.';
  }
  return 'Καμία είσπραξη με καταχωρημένο τρόπο και καμία μίσθωση. Χωρίς τεκμηρίωση η έκπτωση δεν εφαρμόζεται.';
}

// ═══════════════════════════════════════════════════════════════════════════
// ΤΟ ΑΛΛΟ ΑΚΙΝΗΤΟ ΤΟΥ ΧΑΡΤΟΦΥΛΑΚΙΟΥ ΚΡΙΝΕΤΑΙ ΜΕ ΤΑ ΔΙΚΑ ΤΟΥ ΣΤΟΙΧΕΙΑ
// ─────────────────────────────────────────────────────────────────────────
// ΤΟ ΣΦΑΛΜΑ. Για φυσικό πρόσωπο ο φόρος ενοικίων είναι προοδευτικός στο ΣΥΝΟΛΟ
// του Ε1, οπότε η καρτέλα Λογιστική χτίζει κατάσταση για ΚΑΘΕ ακίνητο του
// χαρτοφυλακίου. Σε όλες όμως περνούσε τον τρόπο είσπραξης ΤΟΥ ΑΝΟΙΧΤΟΥ
// ακινήτου. Δύο συνέπειες — κι οι δύο σε νούμερα που φεύγουν στον λογιστή:
//
//   · Ακίνητο που εισπράττεται σε ΜΕΤΡΗΤΑ έπαιρνε την τεκμαρτή έκπτωση 5%
//     επειδή ο χρήστης είχε ανοιχτή την καρτέλα ενός άλλου που εισπράττεται
//     μέσω τραπέζης. Δηλαδή φόρος μικρότερος από τον οφειλόμενο.
//   · Και το αντίστροφο: ο φόρος ΑΛΛΑΖΕ ανάλογα με το ποια καρτέλα κοιτάς,
//     χωρίς τίποτα να αλλάξει στα δεδομένα.
//
// Ο ΚΑΝΟΝΑΣ: Η ΑΠΟΔΕΙΞΗ ΤΟΥ ΙΔΙΟΥ ΤΟΥ ΑΚΙΝΗΤΟΥ ΝΙΚΑ. Οταν οι εισπράξεις του
// έχουν καταγεγραμμένο τρόπο, αυτές μιλούν. Οταν δεν έχουν, ΔΕΝ πέφτουμε στο
// «άγνωστο» —που σημαίνει καμία έκπτωση— γιατί η πρόθεση της μίσθωσης
// (`tenants.e_payment`) φορτώνεται μόνο για το ανοιχτό ακίνητο: θα στερούσαμε
// την έκπτωση από ακίνητα για τα οποία απλώς δεν ρωτήσαμε. Σε αυτή τη μόνη
// περίπτωση ισχύει η απάντηση που έδωσε ο χρήστης στην οθόνη.
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Εισπράττεται μέσω τραπέζης ΑΥΤΟ το ακίνητο;
 *
 * `rows`: οι εισπράξεις ΤΟΥ ακινήτου. `fallback`: η απάντηση της οθόνης, που
 * ισχύει μόνο όταν το ίδιο το ακίνητο δεν έχει καμία είσπραξη με τρόπο.
 */
export function collectionViaBankOf(
  rows: readonly RentPaidRow[],
  year: number,
  fallback: boolean,
): boolean {
  const mode = rentCollectionMode(rows, year, null);
  return mode.basis === 'payments' ? mode.viaBank : fallback;
}
