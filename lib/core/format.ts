// ═══════════════════════════════════════════════════════════════════════════
// ΟΙ ΑΡΙΘΜΟΙ, ΣΤΑ ΕΛΛΗΝΙΚΑ. ΜΙΑ ΦΟΡΑ.
// ─────────────────────────────────────────────────────────────────────────
// Ζουν στο `lib/` και όχι στο `components/`, ώστε να τους φτάνει και ο πυρήνας:
// η μηχανή insights, οι υπολογισμοί δανείων και οι αποδόσεις παράγουν κείμενο
// που βλέπει ο χρήστης, αλλά το `lib/` ΔΕΝ επιτρέπεται να εισάγει από το
// `components/` — θα αντέστρεφε τη ροή των εξαρτήσεων. Το `components/tokens.ts`
// τους επανεξάγει, οπότε οι οθόνες συνεχίζουν να γράφουν `from '@/components/Theme'`.
//
// ΓΙΑΤΙ ΥΠΑΡΧΟΥΝ
//
// Το `toFixed(1)` γράφει ΠΑΝΤΑ τελεία, ανεξάρτητα από γλώσσα. Βρέθηκαν 31 σημεία
// που έδειχναν «4.2%» δίπλα σε ποσά «1.234,56€»: δύο συστήματα αρίθμησης στην
// ίδια γραμμή. Στα ελληνικά η τελεία είναι χωριστής ΧΙΛΙΑΔΩΝ, οπότε το «4.200»
// διαβαζόταν ως τέσσερις χιλιάδες διακόσια. Πέντε ακόμη σημεία το είχαν διορθώσει
// χειροκίνητα με `.replace('.', ',')` — πέντε υλοποιήσεις του ίδιου πράγματος,
// που είναι ο άλλος τρόπος να διαφωνήσουν δύο οθόνες.
// ═══════════════════════════════════════════════════════════════════════════

const LOCALE = 'el-GR';
const finite = (n: unknown): number => (typeof n === 'number' && Number.isFinite(n) ? n : 0);

// ── ΔΥΟ ΨΗΦΙΑ. ΠΑΝΤΑ. ─────────────────────────────────────────────────────
// Οι στήλες μιας οικονομικής κατάστασης στοιχίζονται στην υποδιαστολή. Όταν η
// μία γραμμή γράφει «751€» και η επόμενη «19,25€», η υποδιαστολή κάθεται σε
// άλλη θέση και η στήλη σπάει: το μάτι δεν μπορεί πια να συγκρίνει κάθετα και
// η σελίδα μοιάζει με σημειωματάριο αντί για κατάσταση τράπεζας.
//
// Γι' αυτό το πλήθος δεκαδικών ΔΕΝ είναι επιλογή του κάθε σημείου. Οι 117
// κλήσεις `fe(x, 0)` και οι 134 `feAuto` δεν χρειάστηκε να αλλάξουν μία-μία —
// άλλαξε ο τύπος και άλλαξαν όλες. Αυτός είναι ο λόγος που υπάρχει ένα σημείο.
//
// ΚΑΙ ΤΟ ΔΕΥΤΕΡΟ ΟΡΙΣΜΑ ΕΦΥΓΕ ΑΠΟ ΤΗΝ ΥΠΟΓΡΑΦΗ. Κρατήθηκε αρχικά για να μη
// σπάσει καμία κλήση, αλλά αγνοούνταν — και μια σιωπηλή παράμετρος είναι χειρότερη
// από μια σπασμένη κλήση: είκοσι σημεία έγραφαν `fe(x, 0)` ή `fe(x, 4)` και
// πίστευαν ότι διάλεγαν ακρίβεια. Δύο φορές έκρυψε πραγματικό σφάλμα — οι τιμές
// ρεύματος και οι τιμές αερίου, που θέλουν τέσσερα δεκαδικά, στρογγυλοποιούνταν
// στα δύο και δώδεκα διαφορετικά τιμολόγια εμφανίζονταν με ΙΔΙΑ τιμή.
// Τώρα ο μεταγλωττιστής δείχνει κάθε τέτοιο σημείο. Για μοναδιαίες τιμές
// υπάρχει το `feRate`.
const MONEY = { minimumFractionDigits: 2, maximumFractionDigits: 2 } as const;

/**
 * ΤΟ ΚΕΝΟ ΠΡΙΝ ΑΠΟ ΤΟ ΕΥΡΩ ΕΙΝΑΙ ΑΔΙΑΣΠΑΣΤΟ.
 *
 * Με απλό κενό, το «1.234,50» και το «€» χωρίζονται σε αλλαγή γραμμής: σε
 * στενή στήλη ή σε κινητό, το σύμβολο πέφτει μόνο του στην επόμενη σειρά και το
 * ποσό διαβάζεται σαν γυμνός αριθμός. Είναι από τις πρώτες λεπτομέρειες που
 * ξεχωρίζουν ένα προσεγμένο από ένα βιαστικό προϊόν και τέσσερις τοπικοί
 * μορφοποιητές το είχαν ήδη σωστά — απλώς ο κανονικός όχι.
 *
 * Ο αναλυτής ποσών (lib/core/greek.ts) δέχεται και τα δύο κενά, οπότε ό,τι
 * γράφεται εδώ ξαναδιαβάζεται σωστά από τη σάρωση και τις εισαγωγές.
 *
 * `fe(1234.5)` → «1.234,50€». Πάντα δύο δεκαδικά.
 */
export const fe = (n: number) =>
  `${finite(n).toLocaleString(LOCALE, MONEY)}€`;

/**
 * Ήταν τα «έξυπνα» δεκαδικά — ακέραιο χωρίς υποδιαστολή, αλλιώς δύο ψηφία.
 * Ακριβώς αυτό έσπαγε τη στοίχηση. Μένει ως όνομα για να μη χρειαστεί άγγιγμα
 * σε 134 σημεία, αλλά είναι πλέον ΤΟ ΙΔΙΟ με το `fe`.
 */
export const feAuto = fe;

/** Ποσοστό: `fp(4.25)` → «4,25%». Πάντα δύο δεκαδικά, για τον ίδιο λόγο. */
export const fp = (n: number) =>
  `${finite(n).toLocaleString(LOCALE, MONEY)}%`;

// ── ΤΟ ΛΟΓΙΣΤΙΚΟ ΠΡΟΣΗΜΟ ───────────────────────────────────────────────────
// Σε κατάσταση αποτελεσμάτων το αρνητικό γράφεται με ΤΥΠΟΓΡΑΦΙΚΟ μείον (U+2212),
// όχι με το ενωτικό του πληκτρολογίου (U+002D): το ενωτικό είναι στενότερο και
// κάθεται πιο ψηλά, οπότε σε στήλη με tabular ψηφία η στήλη σπάει οπτικά. Και
// γράφεται ΣΦΙΧΤΑ πριν τον αριθμό, χωρίς κενό.
//
// Υπήρχε ήδη, γραμμένο δύο φορές — `rSigned` για τις εκτυπώσεις και `pSigned`
// για τα επίσημα PDF — και ΚΑΜΙΑ από τις δύο δεν περνούσε από τη φρουρά του
// `finite`: ένα χαλασμένο ποσό τυπωνόταν «NaN €» πάνω σε έγγραφο με αριθμό
// εγγράφου και κωδικό QR επαλήθευσης.
/** Λογιστικό ποσό: `feSigned(-751)` → «−751,00€» (τυπογραφικό μείον, σφιχτό). */
export const feSigned = (n: number | null | undefined): string => {
  const v = finite(n);
  return v < 0 ? `−${fe(Math.abs(v))}` : fe(v);
};

/**
 * Λογιστικό ποσοστό: `fpSigned(-29.63)` → «−29,63%».
 *
 * ΤΟ ΠΡΟΣΗΜΟ ΤΟ ΕΒΑΖΕ Ο ΚΑΘΕΝΑΣ ΜΟΝΟΣ ΤΟΥ, ΚΑΙ ΜΕ ΤΟ ΛΑΘΟΣ ΣΗΜΑΔΙ. Το `fp`
 * περνά από το `toLocaleString`, που για τα ελληνικά γράφει το ΕΝΩΤΙΚΟ του
 * πληκτρολογίου (U+002D): στενότερο και ψηλότερα από το τυπογραφικό μείον
 * (U+2212), δηλαδή ακριβώς το σφάλμα που το `feSigned` υπάρχει για να λύσει στα
 * ποσά. Στην ίδια πρόταση έβγαινε «57,00€» με σωστά σημεία και «-29,63%» με
 * ενωτικό, τρεις λέξεις μακριά.
 */
export const fpSigned = (n: number | null | undefined): string => {
  const v = finite(n);
  return v < 0 ? `−${fp(Math.abs(v))}` : fp(v);
};

// ── Η ΜΟΝΑΔΙΑΙΑ ΤΙΜΗ ΔΕΝ ΕΙΝΑΙ ΠΟΣΟ ────────────────────────────────────────
// Ο κανόνας των δύο δεκαδικών υπάρχει για να στοιχίζονται οι στήλες ποσών. Μια
// τιμή μονάδας δεν κάθεται ποτέ σε τέτοια στήλη: γράφεται μία φορά, μέσα σε
// πρόταση και πολλαπλασιάζεται με κατανάλωση. Η τιμή ρεύματος στην Ελλάδα είναι
// της τάξης 0,1150€/kWh — στρογγυλεμένη στα δύο ψηφία γίνεται 0,12€/kWh και
// τότε η οθόνη λέει ταυτόχρονα «0,12€/kWh» και ένα σύνολο υπολογισμένο στα
// 0,1150. Οι δύο αριθμοί δεν βγάζουν ο ένας τον άλλον και ο χρήστης που κάνει
// τον πολλαπλασιασμό βρίσκει διαφορά τεσσάρων τοις εκατό.
//
// Άρα: δύο ψηφία κατ' ελάχιστον (η στοίχιση διατηρείται), τέσσερα κατ' ανώτατο
// (η ακρίβεια της τιμολόγησης διατηρείται). Δεν είναι εξαίρεση στον κανόνα: ο
// κανόνας αφορά ποσά και αυτό δεν είναι ποσό.
/** Μοναδιαία τιμή: `feRate(0.115)` → «0,115€». Δύο έως τέσσερα δεκαδικά. */
export const feRate = (n: number) =>
  `${finite(n).toLocaleString(LOCALE, { minimumFractionDigits: 2, maximumFractionDigits: 4 })}€`;

/**
 * ΣΥΜΠΑΓΕΣ ΠΟΣΟ ΓΙΑ ΣΤΕΝΟ ΧΩΡΟ — ετικέτες ράβδων, tooltips, άξονες γραφημάτων.
 *
 * Υπήρχαν δύο, με διαφορετική γλώσσα: η οθόνη απόδοσης έγραφε «1,2 εκατ. €»,
 * και η αναφορά PDF «1.2k €» — λατινικό σύμβολο μέσα σε ελληνικό έγγραφο που
 * παραδίδεται σε λογιστή. Και η μία δεν χειριζόταν καθόλου τις χιλιάδες.
 *
 * Οι μονάδες γράφονται συντετμημένες ΜΟΝΟ εδώ και μόνο επειδή ο χώρος είναι
 * κυριολεκτικά μια ετικέτα ράβδου: οπουδήποτε αλλού γράφεται το πλήρες ποσό.
 */
export const feCompact = (n: number): string => {
  const v = finite(n), a = Math.abs(v);
  const u = (x: number, label: string) =>
    `${(v / x).toLocaleString(LOCALE, { maximumFractionDigits: 1 })}\u00A0${label}€`;
  if (a >= 1e12) return u(1e12, 'τρισ.');
  if (a >= 1e9) return u(1e9, 'δισ.');
  if (a >= 1e6) return u(1e6, 'εκατ.');
  if (a >= 1e4) return u(1e3, 'χιλ.');
  return fe(v);
};

/**
 * ΑΚΕΡΑΙΟ ΠΟΣΟ ΓΙΑ ΚΕΛΙ ΠΛΕΓΜΑΤΟΣ — τιμή νύχτας στο ημερολόγιο τιμολόγησης.
 *
 * ΓΙΑΤΙ ΟΧΙ ΤΟ `fe`. Ο κανόνας των δύο δεκαδικών υπάρχει για να στοιχίζονται οι
 * ΣΤΗΛΕΣ ποσών. Το κελί του ημερολογίου δεν είναι στήλη: είναι τετράγωνο 42
 * εικονοστοιχείων με τον αριθμό της ημέρας από πάνω και την τιμή από κάτω,
 * κεντραρισμένα και τα δύο. Το «205,00€» ζητά 55 εκεί που υπάρχουν 42, οπότε ή
 * κόβεται ή μικραίνει η γραμματοσειρά κάτω από το δάπεδο των έντεκα.
 *
 * ΚΑΙ ΓΙΑΤΙ ΟΧΙ ΓΥΜΝΟΣ ΑΡΙΘΜΟΣ, ΟΠΩΣ ΗΤΑΝ. Το «205» δίπλα στο «18» της ημέρας
 * δεν λέει από μόνο του τι είναι· η μονάδα ζούσε μόνο στο υπόμνημα από κάτω και
 * στην ετικέτα προσιτότητας. Ενα ποσό στην οθόνη γράφει τη μονάδα του.
 *
 * Οι τιμές νύχτας είναι ακέραια ευρώ — δεν υπάρχει «205,40€ η νύχτα» σε κανένα
 * κανάλι κράτησης — οπότε τα δύο δεκαδικά δεν κρύβουν πληροφορία εδώ.
 *
 * `feWhole(205)` → «205€».
 */
export const feWhole = (n: number) =>
  `${Math.round(finite(n)).toLocaleString(LOCALE, { maximumFractionDigits: 0 })}€`;

/** Αριθμός χωρίς μονάδα: `fn(1234.5, 1)` → «1.234,5». */
export const fn = (n: number, d = 0) =>
  finite(n).toLocaleString(LOCALE, { minimumFractionDigits: d, maximumFractionDigits: d });

// ── ΚΑΜΙΑ ΠΑΥΛΑ ΜΕΣΑ ΣΕ ΣΤΗΛΗ ΑΡΙΘΜΩΝ ─────────────────────────────────────
// Η παύλα είναι τυπογραφικά ένα ΣΥΜΒΟΛΟ, όχι αριθμός: δεν έχει υποδιαστολή, δεν
// στοιχίζεται με τίποτα και κάθε φορά που εμφανίζεται σε στήλη ποσών τρυπάει τη
// γραμμή. Δέκα σειρές με «1.234,50€» και μία με «—» δεν διαβάζονται ως πίνακας.
//
// Οπότε ο άγνωστος αριθμός γράφεται «0,00€». Το ότι δεν υπάρχει μέτρηση
// ΕΞΑΚΟΛΟΥΘΕΙ να λέγεται — αλλά από τη διάταξη, όχι από τον χαρακτήρα: όταν
// ΚΑΝΕΝΑ πλακίδιο μιας σειράς δεν μετράει κάτι, η σειρά δεν εμφανίζεται
// καθόλου (βλ. `isBlankMetric` και KPIGrid). Η απουσία λέγεται με απουσία.
/**
 * @deprecated Η παύλα δεν γράφεται πουθενά στην οθόνη. Για ποσά και ποσοστά
 * γράφεται «0,00€» / «0,00%»· για κείμενο, μία από τις λέξεις παρακάτω.
 * Μένει εξαγόμενη μόνο για να μη σπάσει ό,τι δεν έχει μεταφερθεί ακόμη.
 */
export const DASH = '—';

// ── ΟΙ ΛΕΞΕΙΣ ΤΗΣ ΑΠΟΥΣΙΑΣ ────────────────────────────────────────────────
// Όταν λείπει ΚΕΙΜΕΝΟ (κατάσταση, όροφος, σημείωση), το κενό δεν μπορεί να
// γεμίσει με μηδέν. Ως τώρα κάθε οθόνη έβαζε παύλα, που δεν είναι λέξη: δεν
// διαβάζεται, δεν στοιχίζεται και σε στήλη κειμένου φαίνεται σαν σφάλμα.
//
// Οι λέξεις ορίζονται ΕΔΩ ώστε να είναι οι ίδιες παντού. Δεκαπέντε οθόνες που
// λένε η καθεμία αλλιώς το ίδιο πράγμα («—», «κενό», «άγνωστο», «Ν/Α») είναι ο
// ίδιος θόρυβος με δεκαπέντε μορφές ευρώ.
export const ABSENT = 'Δεν έχει οριστεί';
export const ABSENT_DATE = 'Χωρίς ημερομηνία';
/** Για στενά κελιά πίνακα, όπου η πλήρης φράση θα έσπαγε τη στήλη. */
export const ABSENT_SHORT = 'Εκκρεμεί';

export const feOr = (n: number | null | undefined) =>
  fe(n == null || !Number.isFinite(n) ? 0 : n);
export const fpOr = (n: number | null | undefined) =>
  fp(n == null || !Number.isFinite(n) ? 0 : n);

/**
 * ΜΕΤΡΑΕΙ ΑΥΤΟ ΤΟ ΠΛΑΚΙΔΙΟ ΚΑΤΙ;
 *
 * Δέχεται ΤΥΠΩΜΕΝΗ τιμή — ό,τι θα δει ο χρήστης — και απαντά αν πίσω της υπάρχει
 * μέτρηση. Δουλεύει στο τυπωμένο κείμενο επειδή εκεί καταλήγουν όλες οι μορφές
 * («0,00€», «0,0%», «0 · 0€», «—»): αν ο έλεγχος γινόταν στα δεδομένα, θα
 * έπρεπε να επαναληφθεί σε κάθε οθόνη με τη δική της δομή, δηλαδή δεκατέσσερις
 * φορές — ακριβώς αυτό που αποφεύγουμε.
 *
 * ΔΥΟ ΚΑΝΟΝΕΣ, ΚΑΙ ΟΙ ΔΥΟ ΣΤΕΝΟΙ:
 *   · Παύλα (ή τίποτα) = δεν ξέρουμε.
 *   · Ψηφία που είναι ΟΛΑ μηδενικά = μετρήσαμε και δεν υπάρχει τίποτα.
 * Κείμενο χωρίς ψηφία («Ενεργό», «Ελεύθερο») ΔΕΝ είναι κενό — είναι απάντηση.
 * Χωρίς αυτόν τον τρίτο έλεγχο, μια σειρά με καταστάσεις αντί για αριθμούς θα
 * εξαφανιζόταν ολόκληρη.
 */
export function isBlankMetric(value: string): boolean {
  const v = (value || '').trim();
  // ΟΙ ΔΙΚΕΣ ΜΑΣ ΛΕΞΕΙΣ ΤΗΣ ΑΠΟΥΣΙΑΣ ΜΕΤΡΑΝΕ ΩΣ ΑΠΟΥΣΙΑ.
  // Ο τρίτος κανόνας («κείμενο χωρίς ψηφία είναι απάντηση») γράφτηκε για το
  // «Ενεργό» και το «Ελεύθερο». Έπιανε όμως και το ΔΙΚΟ ΜΑΣ «Δεν έχει οριστεί»:
  // μια σειρά με τέσσερα πλακίδια που ΟΛΑ έλεγαν «Δεν έχει οριστεί» θεωρούνταν
  // ότι «μετράει κάτι» και έμενε στην οθόνη — ακριβώς ο θόρυβος που ήρθε να
  // σβήσει αυτός ο έλεγχος. Οι φράσεις είναι τρεις και ζουν δίπλα.
  if (v === ABSENT || v === ABSENT_DATE || v === ABSENT_SHORT) return true;
  const digits = v.replace(/\D/g, '');
  if (!digits) return v === '' || /^[—–-]+$/.test(v);
  return /^0+$/.test(digits);
}

// ═══════════════════════════════════════════════════════════════════════════
// ΤΑ ΕΛΛΗΝΙΚΑ ΚΕΦΑΛΑΙΑ ΔΕΝ ΠΑΙΡΝΟΥΝ ΤΟΝΟ
// ─────────────────────────────────────────────────────────────────────────
// ΤΟ ΣΦΑΛΜΑ, ΜΕΤΡΗΜΕΝΟ. Το `text-transform: uppercase` του CSS μετατρέπει το
// «ό» σε «Ό» — δηλαδή ΚΡΑΤΑΕΙ τον τόνο. Στην εφαρμογή αυτό συνέβαινε σε κάθε
// ετικέτα ενότητας και σε κάθε σήμα κατάστασης: «ΧΑΡΤΟΦΥΛΆΚΙΌ ΜΟΥ» με δύο
// τόνους σε μία λέξη, «ΈΛΗΞΕ», «ΔΩΡΕΆΝ ΕΡΓΑΛΕΊΟ». Τριάντα μία από τριάντα μία
// επικεφαλίδες ενότητας, είκοσι τρία σήματα.
//
// ΓΙΑΤΙ ΕΔΩ ΚΑΙ ΟΧΙ ΣΕ ΚΑΘΕ ΣΥΜΒΟΛΟΣΕΙΡΑ. Οι πηγές είναι πέντε primitives, οι
// συμβολοσειρές εξήντα αρχεία. Διόρθωση ανά συμβολοσειρά θα ξανάσπαγε στην
// πρώτη νέα ετικέτα που θα έγραφε κάποιος πεζά, όπως πρέπει να τη γράφει.
// Ο μετασχηματισμός ζει στο σημείο που ΑΠΟΦΑΣΙΖΕΙ τα κεφαλαία.
//
// ΤΑ ΔΙΑΛΥΤΙΚΑ ΜΕΝΟΥΝ. Ο κανόνας αφορά τον τόνο, όχι τη διαίρεση της διφθόγγου:
// «ΑΫΠΝΙΑ» κρατά τα διαλυτικά της. Τα «ΐ» και «ΰ» κουβαλούν και τα δύο, οπότε
// χάνουν τον τόνο και κρατούν τα διαλυτικά (Ϊ, Ϋ).
//
// ΤΟ ΤΕΛΙΚΟ ΣΙΓΜΑ. Το `toUpperCase()` της JavaScript το κάνει «Σ» μόνο του, που
// είναι σωστό — δεν χρειάζεται τίποτα εδώ.
// ═══════════════════════════════════════════════════════════════════════════
/**
 * Ελληνικά κεφαλαία χωρίς τόνο, με τα διαλυτικά στη θέση τους.
 *
 * ΓΙΑΤΙ ΟΧΙ ΠΙΝΑΚΑΣ ΑΝΤΙΣΤΟΙΧΙΣΗΣ. Η πρώτη γραφή ήταν
 * `{ 'Ά': 'Α', … 'ΐ': 'Ϊ' }` και άφηνε πίσω της «ΜΑΪ́ΟΥ»: το `toUpperCase()`
 * ΔΙΑΣΠΑ μόνο του το «ΐ» σε «Ϊ» συν συνδυαστικό τόνο, οπότε ο πίνακας δεν
 * έβρισκε ποτέ τον χαρακτήρα που έψαχνε. Ένας τόνος επιβίωνε ακριβώς εκεί που
 * κανείς δεν κοιτούσε.
 *
 * ΤΑ ΔΙΑΛΥΤΙΚΑ ΔΕΝ ΜΠΑΙΝΟΥΝ ΟΠΟΥ ΝΑ ΝΑΙ, ΚΑΙ Η ΣΥΝΑΡΤΗΣΗ ΔΕΝ ΤΑ ΠΡΟΣΘΕΤΕΙ.
 * Χωρίζουν δίφθογγο που θα διαβαζόταν αλλιώς: «ΜΑΪΟΥ», «ΤΑΪΖΩ», «ΑΫΠΝΙΑ» — το
 * «ΑΙ» χωρίς αυτά διαβάζεται «ε». Το «ωι» ΔΕΝ είναι δίφθογγος, οπότε το «πρωί»
 * γίνεται σωστά «ΠΡΩΙ»: εδώ απλώς φεύγει ο τόνος. Ο κανόνας βγαίνει μόνος του
 * από τη γραφή της λέξης, γι' αυτό δεν υπάρχει λίστα εξαιρέσεων.
 *
 * Η ΑΠΟΣΥΝΘΕΣΗ ΤΑ ΛΥΝΕΙ ΟΛΑ ΜΑΖΙ. Σε μορφή NFD κάθε τονισμένο φωνήεν γίνεται
 * γράμμα συν οξεία (U+0301). Σβήνουμε την οξεία, ξανασυνθέτουμε. Τα διαλυτικά
 * (U+0308) μένουν άθικτα, γιατί ο κανόνας αφορά τον τόνο και όχι τη διαίρεση
 * της διφθόγγου: η «ΑΫΠΝΙΑ» κρατά τα δικά της.
 *
 * ΤΟ ΤΕΛΙΚΟ ΣΙΓΜΑ γίνεται «Σ» από το ίδιο το `toUpperCase()`, σωστά.
 *
 * Χρησιμοποιείται από κάθε primitive που εφαρμόζει `textTransform: uppercase`.
 * Αν προσθέσεις καινούριο, πέρασε το κείμενο από εδώ — ο φύλακας
 * scripts/guard-uppercase-tonos.mjs δεν σε αφήνει να το ξεχάσεις.
 */
export const grUpper = (v: unknown): string =>
  String(v ?? '').toUpperCase().normalize('NFD').replace(/\u0301/g, '').normalize('NFC');

// ═══════════════════════════════════════════════════════════════════════════
// Η ΗΜΕΡΟΜΗΝΙΑ ΣΕ ΕΛΛΗΝΙΚΗ ΑΝΑΓΝΩΣΗ — ΜΙΑ ΓΡΑΦΗ
// ─────────────────────────────────────────────────────────────────────────
// Ήταν γραμμένη τέσσερις φορές με το ίδιο όνομα, `grDate`, σε τέσσερα αρχεία,
// και μια πέμπτη φορά με το ίδιο όνομα αλλά ΑΛΛΟ αποτέλεσμα («14 Αυγούστου
// 2026» αντί για «14/08/2026»). Πέντε ορισμοί, δύο διαφορετικές έξοδοι, ένα
// όνομα: όποιος διάβαζε τον έναν δεν μπορούσε να ξέρει τι θα δει ο χρήστης.
//
// ΓΙΑΤΙ ΧΩΡΙΣ `new Date`. Η μία από τις τέσσερις χρησιμοποιούσε
// `new Date(iso).toLocaleDateString('el-GR')`. Μια σκέτη ημερομηνία
// («2026-08-14») διαβάζεται ως μεσάνυχτα UTC και τυπώνεται στην ΤΟΠΙΚΗ ζώνη:
// σε κάθε ζώνη δυτικά του Γκρίνουιτς βγαίνει η ΠΡΟΗΓΟΥΜΕΝΗ ημέρα. Στην Ελλάδα
// δεν φαίνεται, σε έναν ιδιοκτήτη που ταξιδεύει φαίνεται. Η γραφή είναι καθαρά
// πάνω στη συμβολοσειρά, οπότε δεν υπάρχει ζώνη να μπερδέψει τίποτα.
//
// Δέχεται και σφραγίδα χρόνου («2026-08-14T09:12:00Z»): κρατά το πρόθεμα της
// ημερομηνίας. Ό,τι δεν αναγνωρίζεται επιστρέφεται ΑΥΤΟΥΣΙΟ — ποτέ κενό και
// ποτέ «Invalid Date», που ήταν η συμπεριφορά που έβγαζε η έκδοση με `Date`.
const ISO_DATE_HEAD = /^(\d{4})-(\d{2})-(\d{2})/;

/** «14/08/2026» από ISO ημερομηνία ή σφραγίδα χρόνου. */
export const grDate = (iso: string | null | undefined): string => {
  const m = iso ? ISO_DATE_HEAD.exec(iso) : null;
  return m ? `${m[3]}/${m[2]}/${m[1]}` : (iso ?? '');
};
