// ═══════════════════════════════════════════════════════════════════════════
// ΤΟ ΣΦΑΛΜΑ ΤΗΣ ΒΑΣΗΣ ΔΕΝ ΤΟ ΔΙΑΒΑΖΕΙ Ο ΙΔΙΟΚΤΗΤΗΣ ΑΚΙΝΗΤΟΥ
// ─────────────────────────────────────────────────────────────────────────
// Πενήντα έξι σημεία της εφαρμογής έδειχναν το `error.message` του Supabase
// κατευθείαν στην οθόνη. Δηλαδή ο Έλληνας ιδιοκτήτης που πάτησε «Αποθήκευση»
// διάβαζε:
//
//   «new row violates row-level security policy for table "expenses"»
//   «duplicate key value violates unique constraint "bills_pkey"»
//   «JWT expired»
//   «Failed to fetch»
//
// Τίποτα από αυτά δεν του λέει τι να κάνει και όλα του λένε ότι κοιτάζει
// μηχανή. Το χειρότερο είναι το τελευταίο: «Failed to fetch» σημαίνει απλώς
// ότι κόπηκε το ίντερνετ — μια κατάσταση που ο χρήστης ΜΠΟΡΕΙ να διορθώσει, αν
// του το πούμε.
//
// ΔΥΟ ΚΑΝΟΝΕΣ:
//
//   1. Η ΟΘΟΝΗ ΞΕΡΕΙ ΤΙ ΑΠΕΤΥΧΕ, Η ΒΑΣΗ ΞΕΡΕΙ ΓΙΑΤΙ. Το κάθε σημείο κλήσης
//      γράφει το «τι» («Η τιμή ρεύματος δεν αποθηκεύτηκε») — αυτό δεν μπορεί
//      να το ξέρει ένας κοινός μεταφραστής. Εδώ προστίθεται μόνο το «γιατί».
//
//   2. ΟΤΑΝ ΔΕΝ ΑΝΑΓΝΩΡΙΖΕΤΑΙ, ΔΕΝ ΛΕΓΕΤΑΙ. Αγνωστος κωδικός δεν μεταφράζεται
//      σε «Κάτι πήγε στραβά» ούτε τυπώνεται στα αγγλικά: μένει το «τι» και μια
//      προτροπή. Το πρωτότυπο κείμενο πάει στην κονσόλα, για όποιον το ψάχνει.
// ═══════════════════════════════════════════════════════════════════════════

import { PASSWORD_MIN_LABEL } from '@/lib/auth/password'
import { captureError, worthReporting } from '@/lib/observability/report';
/** Ο,τι επιστρέφει ο πελάτης Supabase ως σφάλμα, ή σκέτο Error. */
interface DbLike { code?: string | null; message?: string | null; details?: string | null }

const asDb = (e: unknown): DbLike =>
  e && typeof e === 'object' ? (e as DbLike) : { message: typeof e === 'string' ? e : null };

/**
 * ΚΩΔΙΚΟΙ POSTGRES ΚΑΙ POSTGREST. Ο κωδικός είναι σταθερός· το κείμενο του
 * σφάλματος αλλάζει με την έκδοση, οπότε ο κωδικός ελέγχεται πρώτος.
 */
const BY_CODE: Record<string, string> = {
  '23505': 'Υπάρχει ήδη καταχώρηση με αυτά τα στοιχεία.',
  '23503': 'Η εγγραφή συνδέεται με κάτι που δεν υπάρχει πια. Ανανέωσε τη σελίδα.',
  '23502': 'Λείπει ένα υποχρεωτικό στοιχείο.',
  '23514': 'Κάποια τιμή είναι εκτός των επιτρεπτών ορίων.',
  '22P02': 'Κάποια τιμή δεν έχει τη σωστή μορφή.',
  '22003': 'Ο αριθμός είναι πολύ μεγάλος για αυτό το πεδίο.',
  '42501': 'Δεν έχεις δικαίωμα σε αυτή την εγγραφή.',
  '42P01': 'Η ενέργεια δεν είναι διαθέσιμη αυτή τη στιγμή.',
  // Ο κωδικός που εξαφάνιζε ολόκληρες οθόνες: ερώτημα με στήλη που δεν υπάρχει
  // απορρίπτεται ΟΛΟΚΛΗΡΟ και η οθόνη δείχνει κενό αντί για σφάλμα.
  '42703': 'Η εφαρμογή ζήτησε πεδίο που δεν υπάρχει στη βάση. Χρειάζεται ενημέρωση.',
  '57014': 'Η ενέργεια άργησε πολύ και διακόπηκε. Δοκίμασε ξανά.',
  '53300': 'Ο διακομιστής δέχεται πολλά αιτήματα. Δοκίμασε ξανά σε λίγο.',
  'PGRST301': 'Η σύνδεσή σου έληξε. Μπες ξανά.',
  'PGRST116': 'Η εγγραφή δεν βρέθηκε. Μπορεί να διαγράφηκε από αλλού.',
};

/**
 * ΚΕΙΜΕΝΑ ΠΟΥ ΔΕΝ ΕΧΟΥΝ ΚΩΔΙΚΟ: δίκτυο, GoTrue (σύνδεση/εγγραφή), όρια ρυθμού.
 * Η σειρά μετράει — το πρώτο που ταιριάζει κερδίζει.
 */
const BY_TEXT: [RegExp, string][] = [
  [/failed to fetch|networkerror|network request failed|load failed/i,
    'Δεν υπάρχει σύνδεση στο διαδίκτυο. Έλεγξε το δίκτυο και δοκίμασε ξανά.'],
  // Το ειδικό πριν το γενικό: το «Email rate limit exceeded» περιέχει «rate
  // limit», οπότε ο γενικός κανόνας το άρπαζε και ο χρήστης δεν μάθαινε ότι
  // πρόκειται για τα μηνύματα και όχι για τις προσπάθειες εισόδου.
  // Χωρίς «στάλθηκαν»: μπαίνει μετά από «…δεν στάλθηκε» και έβγαινε
  // «Το μήνυμα δεν στάλθηκε. Στάλθηκαν πολλά μηνύματα.»
  [/email rate limit|over_email_send_rate/i,
    'Ζητήθηκαν πολλά μηνύματα σε λίγο χρόνο. Δοκίμασε ξανά σε ένα λεπτό.'],
  [/rate limit|too many requests|over_request_rate/i,
    'Πολλές προσπάθειες σε μικρό διάστημα. Δοκίμασε ξανά σε λίγο.'],
  // «email» σε πρόταση, όπως κάθε άλλη πρόταση των οθονών εισόδου («Άνοιξε το
  // email σου»). Η ετικέτα του πεδίου μένει «Ηλεκτρονικό ταχυδρομείο»: είναι
  // όνομα πεδίου και εκεί ο φύλακας των ξένων λέξεων ζητά το ελληνικό.
  [/invalid login credentials/i, 'Λάθος email ή κωδικός.'],
  [/user already registered|already been registered/i,
    'Υπάρχει ήδη λογαριασμός με αυτό το email.'],
  [/email not confirmed/i, 'Δεν έχεις επιβεβαιώσει ακόμη το email σου.'],
  [/token has expired|otp_expired|expired/i, 'Ο σύνδεσμος έληξε. Ζήτησε καινούριο.'],
  [/at least|password.*(weak|short)|6 char/i,
    `Ο κωδικός είναι πολύ αδύναμος. Χρειάζεται ${PASSWORD_MIN_LABEL.toLowerCase()}.`],
  [/row-level security|violates row.level/i, 'Δεν έχεις δικαίωμα σε αυτή την εγγραφή.'],
  [/jwt|not authenticated|session.*(missing|expired)/i, 'Η σύνδεσή σου έληξε. Μπες ξανά.'],
  [/aborted|timeout|timed out/i, 'Η ενέργεια άργησε πολύ. Δοκίμασε ξανά.'],
];

/**
 * Ο λόγος σε ελληνικά, ή `null` όταν δεν αναγνωρίζεται.
 * Ποτέ δεν επιστρέφει το αγγλικό κείμενο του σφάλματος.
 */
export function dbReason(e: unknown): string | null {
  const d = asDb(e);
  const code = (d.code ?? '').toString().trim();
  if (code && BY_CODE[code]) return BY_CODE[code];
  const text = `${d.message ?? ''} ${d.details ?? ''}`;
  if (!text.trim()) return null;
  for (const [re, gr] of BY_TEXT) if (re.test(text)) return gr;
  return null;
}

/** Η προτροπή όταν ο λόγος είναι άγνωστος. Μία, παντού. */
export const RETRY = 'Δοκίμασε ξανά.';

/**
 * ΤΟ ΜΗΝΥΜΑ ΠΟΥ ΔΙΑΒΑΖΕΙ Ο ΧΡΗΣΤΗΣ.
 *
 *   failed('Η τιμή ρεύματος δεν αποθηκεύτηκε', error)
 *     → «Η τιμή ρεύματος δεν αποθηκεύτηκε. Δεν έχεις δικαίωμα σε αυτή την εγγραφή.»
 *     → «Η τιμή ρεύματος δεν αποθηκεύτηκε. Δοκίμασε ξανά.»  (άγνωστος λόγος)
 *
 * Το `what` γράφεται ΧΩΡΙΣ τελεία: την προσθέτει η συνάρτηση, ώστε τα 199
 * μηνύματα της εφαρμογής να μη διαφωνούν στη στίξη.
 */
export function failed(what: string, e?: unknown): string {
  // ═══ ΤΟ ΜΗΝΥΜΑ ΚΑΙ Η ΑΝΑΦΟΡΑ ΕΙΝΑΙ ΤΟ ΙΔΙΟ ΓΕΓΟΝΟΣ ═══════════════════════
  // ΜΕΤΡΗΘΗΚΕ: 219 μπλοκ `catch` σε κώδικα παραγωγής και ΚΑΝΕΝΑ δεν ανέφερε
  // πουθενά. Το `captureError` καλούνταν από τρία σημεία — και τα τρία όρια
  // σφάλματος της React — που πιάνουν σφάλμα ΑΠΟΔΟΣΗΣ. Οι αποτυχίες που
  // πονάνε σε εφαρμογή χρημάτων δεν είναι αυτές: είναι η αποθήκευση που δεν
  // έγινε, το ανέβασμα που κόπηκε, η γραμμή που δεν γράφτηκε. Ολες αυτές
  // περνούν από ΕΔΩ, γιατί από εδώ βγαίνει το μήνυμα που τις λέει στον χρήστη.
  //
  // ΓΙΑΤΙ ΕΔΩ ΚΑΙ ΟΧΙ ΣΕ 219 ΣΗΜΕΙΑ. Το να προστεθεί μια κλήση σε κάθε `catch`
  // είναι 219 ευκαιρίες να ξεχαστεί η μία — και η μία που ξεχνιέται είναι
  // πάντα αυτή που θα χρειαστεί. Οποιος λέει στον χρήστη «δεν αποθηκεύτηκε»
  // το λέει με αυτή τη συνάρτηση· άρα εδώ είναι το σημείο όπου η αναφορά ΔΕΝ
  // μπορεί να ξεχαστεί.
  //
  // ΑΝΑΦΕΡΕΤΑΙ ΜΟΝΟ ΟΤΑΝ ΥΠΑΡΧΕΙ ΣΦΑΛΜΑ. Το `failed('Το PDF δεν δημιουργήθηκε')`
  // χωρίς δεύτερο όρισμα είναι δήλωση προς τον χρήστη, όχι συμβάν προς
  // διερεύνηση: δεν υπάρχει στοίβα, δεν υπάρχει τίποτα να σταλεί.
  //
  // ΔΕΝ ΚΟΣΤΙΖΕΙ ΤΙΠΟΤΑ ΧΩΡΙΣ DSN: το `captureError` βγαίνει αμέσως όταν δεν
  // υπάρχει ρυθμισμένος προορισμός· έχει επίσης δικό του φίλτρο επανάληψης (τρεις
  // φορές το ίδιο αποτύπωμα, διακόσια αποτυπώματα) ώστε ένας βρόχος που σκάει
  // να μη γεμίσει τη δωρεάν βαθμίδα σε ένα απόγευμα.
  if (e !== undefined && worthReporting(e)) captureError(e, { failed: what });
  const base = what.trim().replace(/[.:·]+$/, '');
  return `${base}. ${dbReason(e) ?? RETRY}`;
}

// ── ΜΗΝΥΜΑΤΑ ΠΟΥ ΕΠΑΝΑΛΑΜΒΑΝΟΝΤΑΝ ΑΥΤΟΥΣΙΑ ΣΕ ΠΟΛΛΕΣ ΟΘΟΝΕΣ ──────────────
// Γραμμένα με το χέρι σε κάθε σημείο, άρα υποψήφια να αποκλίνουν στην επόμενη
// διόρθωση. Η αποθήκευση της σύνδεσης με το ημερολόγιο λεγόταν ήδη με ΔΥΟ
// τρόπους — «Ο σύνδεσμος με το ημερολόγιο» (τρεις φορές) και «Η σύνδεση με το
// ημερολόγιο» (δύο) — μέσα στο ίδιο αρχείο. Και τα δύο δεν είναι το ίδιο
// πράγμα: σύνδεσμος είναι ο υπερσύνδεσμος, σύνδεση είναι ο δεσμός των δύο
// εγγραφών. Εδώ είναι το δεύτερο.
export const MSG = {
  pdf: 'Το επίσημο PDF δεν δημιουργήθηκε',
  upload: 'Η μεταφόρτωση δεν ολοκληρώθηκε',
  calendarLink: 'Η σύνδεση με το ημερολόγιο δεν αποθηκεύτηκε',
  // Η υπενθύμιση γράφεται και από τις Επαφές και από τη Συντήρηση εξοπλισμού.
  reminder: 'Η υπενθύμιση δεν μπήκε στο ημερολόγιο',
  // Το γεγονός ενημερώνεται και από τη σάρωση απόδειξης και από τη σάρωση εγγράφου.
  calendarEvent: 'Το γεγονός ημερολογίου δεν ενημερώθηκε',
} as const;

/**
 * ΟΛΟΚΛΗΡΕΣ ΠΡΟΤΑΣΕΙΣ, ΟΧΙ ΑΦΟΡΜΕΣ ΓΙΑ ΤΗΝ `failed`.
 *
 * Ο,τι ζει στο `MSG` γράφεται ΧΩΡΙΣ τελεία, γιατί την προσθέτει η `failed`
 * μαζί με τον λόγο της βάσης. Τα παρακάτω δεν έχουν λόγο βάσης να προσθέσουν:
 * είναι πλήρη μηνύματα που εμφανίζονται αυτούσια και επαναλαμβάνονταν σε δύο
 * οθόνες το καθένα. Κρατούν τη δική τους στίξη και τη γράφουν μία φορά.
 */
export const SAY = {
  tooManyTries: 'Πολλές προσπάθειες. Δοκίμασε ξανά σε λίγο.',
  accountNotDeleted: 'Ο λογαριασμός δεν διαγράφηκε. Δοκίμασε ξανά σε λίγο.',
  changeFailed: 'Δεν ήταν δυνατή η αλλαγή. Δοκίμασε ξανά.',
  archiveFailed: 'Η αρχειοθέτηση απέτυχε. Το PDF έχει ήδη κατέβει.',
  archiveAsk: 'Να αποθηκευτεί στα έγγραφα του ακινήτου;',
  nameRequired: 'Το ονοματεπώνυμο είναι υποχρεωτικό.',
  aiQuotaSpent: 'Εξαντλήθηκαν οι ερωτήσεις του πακέτου σου και ανανεώνονται αυτόματα.',
  // Οι σαρώσεις έχουν δικό τους μετρητή (bump_scan_usage), όχι τις ερωτήσεις:
  // εφεδρεία όταν ο διακομιστής δεν έστειλε το δικό του κείμενο.
  scanQuotaSpent: 'Χρησιμοποίησες τις σαρώσεις του μήνα. Ανανεώνονται την 1η του επόμενου μήνα.',
} as const;
