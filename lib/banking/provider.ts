// ═══════════════════════════════════════════════════════════════════════════
// ΠΟΙΟΣ ΠΑΡΟΧΟΣ, ΚΑΙ ΤΙ ΓΙΝΕΤΑΙ ΟΣΟ ΔΕΝ ΥΠΑΡΧΕΙ ΚΑΝΕΝΑΣ
// ─────────────────────────────────────────────────────────────────────────
// ΤΟ ΣΗΜΕΙΟ ΟΠΟΥ Η ΕΝΤΙΜΟΤΗΤΑ ΚΟΣΤΙΖΕΙ ΚΩΔΙΚΑ. Ολα όσα προηγούνται — τύποι,
// συγκατάθεση, κανονικοποίηση, σχήμα βάσης, απομόνωση — είναι γραμμένα και
// δοκιμασμένα. Αυτό που λείπει είναι το τελευταίο κομμάτι: οι ΠΡΑΓΜΑΤΙΚΕΣ
// διευθύνσεις και τα ΠΡΑΓΜΑΤΙΚΑ ονόματα πεδίων ενός συγκεκριμένου παρόχου.
//
// Αυτά ΔΕΝ γράφονται από μνήμη. Ενας προσαρμογέας με επινοημένα endpoints
// μεταγλωττίζεται, περνά τα τεστ και σπάει την πρώτη φορά που θα τον καλέσει
// αληθινός χρήστης — με μήνυμα που δεν εξηγεί τίποτα. Χειρότερα: αν έστω ένα
// όνομα πεδίου είναι λάθος, ο προσαρμογέας θα διάβαζε `undefined` και θα
// εισήγαγε κινήσεις με λάθος ποσά ή λάθος ημερομηνίες. Το βιβλίο θα έδειχνε
// αριθμούς. Απλώς λάθος.
//
// Γι' αυτό εδώ δεν υπάρχει προσαρμογέας. Υπάρχει μητρώο και μια άρνηση που
// λέει ονομαστικά τι λείπει.
//
// ── ΤΙ ΧΡΕΙΑΖΕΤΑΙ ΓΙΑ ΝΑ ΑΝΟΙΞΕΙ ────────────────────────────────────────
// 1. Λογαριασμός σε αδειοδοτημένο πάροχο AISP και τα διαπιστευτήριά του ως
//    μεταβλητές περιβάλλοντος στο Vercel — ΠΟΤΕ σε συνομιλία, ποτέ στον κώδικα.
// 2. Η επίσημη τεκμηρίωση του παρόχου, ώστε ο προσαρμογέας να γραφτεί από
//    αυτήν και όχι από εικασία.
// 3. Τιμή για το πρόσθετο `bank_link` (lib/billing/addons.ts). Οσο είναι
//    `null`, το πρόσθετο δεν προσφέρεται και η σύνδεση δεν ξεκινά.
//
// ── ΓΙΑΤΙ ΟΧΙ «GOCARDLESS ΚΑΙ ΤΕΛΟΣ» ────────────────────────────────────
// Το GoCardless Bank Account Data (πρώην Nordigen) ήταν ο προφανής πρώτος
// σταθμός λόγω δωρεάν βαθμίδας. ΕΚΛΕΙΣΕ ΤΙΣ ΝΕΕΣ ΕΓΓΡΑΦΕΣ τον Ιούλιο 2025 και
// το λέει σε δική του σελίδα· οι υπάρχοντες λογαριασμοί συνεχίζουν να
// δουλεύουν. Ημερομηνία οριστικής απόσυρσης ΔΕΝ έχει επιβεβαιωθεί και δεν
// γράφεται εδώ ως γεγονός. Πρακτικά: χωρίς λογαριασμό από πριν, δεν ανοίγει.
//
// ── ΠΟΙΟΝ ΔΕΙΧΝΕΙ ΣΗΜΕΡΑ Η ΕΡΕΥΝΑ ───────────────────────────────────────
// Το `enablebanking` είναι πρώτο στη λίστα των επιτρεπτών τιμών γιατί είναι το
// μόνο που επιτρέπει να ΓΡΑΦΤΕΙ ΚΑΙ ΝΑ ΔΟΚΙΜΑΣΤΕΙ αυτός ο προσαρμογέας πριν
// υπάρξει εταιρεία: εγγραφή χωρίς πωλητή, δωρεάν sandbox και «Restricted
// Production» με ΠΡΑΓΜΑΤΙΚΑ δεδομένα σε λογαριασμούς που συνδέει ο ίδιος ο
// κάτοχος. Είναι αδειοδοτημένο AISP υπό τη FIN-FSA (Φινλανδία), με τεκμηριωμένη
// κάλυψη Εθνικής, Πειραιώς, Alpha και Eurobank και χρεώνει ανά συνδεδεμένο
// λογαριασμό τον μήνα — δηλαδή ακριβώς τη μονάδα του πρόσθετου `bank_link`.
//
// Η ΕΠΙΛΟΓΗ ΠΑΡΑΜΕΝΕΙ ΑΝΟΙΧΤΗ και ζει σε μεταβλητή περιβάλλοντος: όποιος και να
// είναι ο πάροχος, η υπόλοιπη εφαρμογή δεν τον ξέρει. Για ΠΛΗΡΗ παραγωγή κάθε
// αδειοδοτημένος πάροχος ζητά σύμβαση και έλεγχο KYB νομικού προσώπου.
// ═══════════════════════════════════════════════════════════════════════════

import { AIS_PROVIDERS, type AisProvider, type AisProviderId } from './types';

/** Η μεταβλητή που λέει ποιον πάροχο χρησιμοποιούμε. */
export const PROVIDER_ENV = 'AIS_PROVIDER';

/** Τα διαπιστευτήρια του παρόχου. Ζουν στον διακομιστή, πουθενά αλλού. */
export const CREDENTIAL_ENV = ['AIS_CLIENT_ID', 'AIS_CLIENT_SECRET'] as const;

type Env = Record<string, string | undefined>;

const isProviderId = (v: string): v is AisProviderId =>
  (AIS_PROVIDERS as readonly string[]).includes(v);

/**
 * Οι προσαρμογείς που υπάρχουν πραγματικά.
 *
 * ΕΙΝΑΙ ΚΕΝΟ ΕΠΙΤΗΔΕΣ, ΚΑΙ ΑΥΤΟ ΕΙΝΑΙ Η ΑΛΗΘΕΙΑ ΤΗΣ ΣΤΙΓΜΗΣ. Θα γεμίσει με
 * μία εγγραφή, όταν υπάρξει λογαριασμός σε πάροχο και η τεκμηρίωσή του.
 */
const ADAPTERS: Partial<Record<AisProviderId, () => AisProvider>> = {};

/** Ποιος πάροχος έχει επιλεγεί. `null` όταν δεν έχει επιλεγεί ή είναι άγνωστος. */
export function configuredProviderId(env: Env): AisProviderId | null {
  const raw = (env[PROVIDER_ENV] || '').trim().toLowerCase();
  return raw && isProviderId(raw) ? raw : null;
}

/**
 * Τι λείπει για να δουλέψει η σύνδεση. Κενό κείμενο σημαίνει «τίποτα».
 *
 * ΜΙΑ ΠΡΟΤΑΣΗ, ΟΝΟΜΑΣΤΙΚΗ. «Δεν είναι διαθέσιμο» δεν λέει σε κανέναν τι να
 * κάνει· «λείπει η μεταβλητή AIS_CLIENT_SECRET» λέει.
 */
export function aisConfigError(env: Env): string {
  const raw = (env[PROVIDER_ENV] || '').trim();
  if (!raw) return `Δεν έχει επιλεγεί πάροχος. Όρισε τη μεταβλητή ${PROVIDER_ENV} σε έναν από: ${AIS_PROVIDERS.join(', ')}.`;

  const id = configuredProviderId(env);
  if (!id) return `Ο πάροχος «${raw}» δεν αναγνωρίζεται. Επιτρεπτές τιμές: ${AIS_PROVIDERS.join(', ')}.`;

  const missing = CREDENTIAL_ENV.filter(k => !(env[k] || '').trim());
  if (missing.length) return `Λείπουν τα διαπιστευτήρια του παρόχου: ${missing.join(', ')}.`;

  if (!ADAPTERS[id]) {
    return `Ο προσαρμογέας για τον πάροχο «${id}» δεν έχει γραφτεί ακόμη. Χρειάζεται η επίσημη τεκμηρίωσή του: οι διευθύνσεις και τα ονόματα πεδίων δεν γράφονται από εικασία, γιατί ένα λάθος όνομα θα εισήγαγε κινήσεις με λάθος ποσά.`;
  }
  return '';
}

/**
 * Το σφάλμα που βλέπει ο διακομιστής όταν κάτι καλέσει τον πάροχο ενώ δεν
 * υπάρχει. Ξεχωριστός τύπος, ώστε η διαδρομή να το ξεχωρίζει από αποτυχία
 * δικτύου και να μη γράψει «η τράπεζα δεν απάντησε» για δικό μας κενό.
 */
export class AisNotConfiguredError extends Error {
  constructor(reason: string) {
    super(reason);
    this.name = 'AisNotConfiguredError';
  }
}

/**
 * Ο πάροχος, ή σφάλμα που εξηγεί γιατί δεν υπάρχει.
 *
 * ΔΕΝ ΕΠΙΣΤΡΕΦΕΙ ΠΟΤΕ ΨΕΥΤΙΚΟ ΠΑΡΟΧΟ. Ενας προσαρμογέας που γυρίζει κενές
 * λίστες αντί να σκάσει θα έδειχνε στον χρήστη «καμία κίνηση» — δηλαδή θα
 * έλεγε ψέματα με τη μορφή άδειας οθόνης.
 */
export function getAisProvider(env: Env): AisProvider {
  const reason = aisConfigError(env);
  if (reason) throw new AisNotConfiguredError(reason);
  return ADAPTERS[configuredProviderId(env)!]!();
}
