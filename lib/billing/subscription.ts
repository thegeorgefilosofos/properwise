// ═══════════════════════════════════════════════════════════════════════════
// Η ΣΥΝΔΡΟΜΗ, ΧΩΡΙΣ ΟΝΟΜΑ ΕΜΠΟΡΟΥ
// ─────────────────────────────────────────────────────────────────────────
// ΓΙΑΤΙ ΞΕΧΩΡΙΣΕ ΑΠΟ ΤΟ «lemon.ts». Οι επτά καταστάσεις μιας συνδρομής, το
// «ποιος δικαιούται τι» και το «σε ποια φάση είναι ο πελάτης» δεν ανήκουν σε
// κανέναν πάροχο: είναι δικοί ΜΑΣ κανόνες και δεν αλλάζουν αν αύριο χρεώνει
// άλλος. Οσο ζούσαν σε αρχείο με το όνομα του παρόχου, η κάρτα χρέωσης του
// πίνακα ελέγχου έπρεπε να εισάγει «lemon» για να ρωτήσει κάτι ουδέτερο.
//
// Ο,τι ΑΛΛΑΖΕΙ με τον πάροχο —ο χάρτης παραλλαγών, τα ονόματα γεγονότων, το
// σχήμα του webhook— έμεινε στο «lemon.ts», πίσω από τη θύρα «merchant/». Το
// όριο είναι ακριβώς αυτό: κανόνες εδώ, πρωτόκολλο εκεί.
// ═══════════════════════════════════════════════════════════════════════════
import { PLAN_ORDER, type PlanId, type BillingCycle } from './plans';

/**
 * Οι καταστάσεις συνδρομής της Lemon Squeezy, όπως τις ορίζει η τεκμηρίωσή της.
 * Η λίστα δεν επεκτείνεται από εικασία.
 */
export const MOR_STATUSES = [
  'on_trial', 'active', 'paused', 'past_due', 'unpaid', 'cancelled', 'expired',
] as const;

export type MorStatus = (typeof MOR_STATUSES)[number];

export const isMorStatus = (v: unknown): v is MorStatus =>
  typeof v === 'string' && (MOR_STATUSES as readonly string[]).includes(v);

export interface VariantPlan { plan: PlanId; cycle: BillingCycle }

export interface MorSubscription {
  /** Το αναγνωριστικό της συνδρομής στη Lemon Squeezy. */
  id: string;
  status: MorStatus;
  variantId: string;
  customerId: string;
  /** Ο λογαριασμός μας, από το `custom_data`. `null` όταν λείπει. */
  userId: string | null;
  /** Πότε ανανεώνεται. Κενό όταν δεν ανανεώνεται. */
  renewsAt: string | null;
  /** Πότε λήγει η πρόσβαση. Γεμάτο σε ακύρωση και σε λήξη. */
  endsAt: string | null;
}

// ── ΠΟΤΕ ΙΣΧΥΕΙ Η ΣΥΝΔΡΟΜΗ ────────────────────────────────────────────────
//
// `on_trial`   δοκιμή σε εξέλιξη — η μόνη δωρεάν περίοδος του προϊόντος.
// `active`     πληρωμένη και ενεργή.
// `past_due`   μια ανανέωση απέτυχε και η Lemon Squeezy ξαναδοκιμάζει επί δύο
//              εβδομάδες. Η πρόσβαση ΜΕΝΕΙ: το να κλείσει η πόρτα σε πελάτη
//              που απλώς άλλαξε κάρτα είναι χειρότερο από δύο εβδομάδες πίστωση.
// `cancelled`  ακυρώθηκε η ΜΕΛΛΟΝΤΙΚΗ χρέωση, αλλά η περίοδος που πληρώθηκε
//              τρέχει ακόμη. Η πρόσβαση ΜΕΝΕΙ ώς το `ends_at` και ούτε μέρα
//              παραπάνω. Χωρίς `ends_at` δεν υπάρχει πρόσβαση: δεν εφευρίσκουμε
//              ημερομηνία λήξης για να είμαστε γενναιόδωροι.
// `paused`     η είσπραξη έχει παύσει με τη θέληση του πελάτη. ΔΕΝ δίνει
//              πρόσβαση: το προϊόν δεν έχει δωρεάν βαθμίδα και μια παύση που
//              κρατά τη συνδρομή ανοιχτή θα ήταν ακριβώς αυτό.
// `unpaid`     απέτυχαν και οι τέσσερις προσπάθειες είσπραξης.
// `expired`    τελείωσε.
//
/**
 * Ισχύει η συνδρομή τη δεδομένη στιγμή;
 *
 * @param nowIso Η στιγμή που κρίνεται, σε ISO. Δίνεται από τον καλούντα ώστε ο
 *               έλεγχος να είναι ντετερμινιστικός.
 */
export function isEntitled(sub: { status: MorStatus; endsAt: string | null }, nowIso: string): boolean {
  switch (sub.status) {
    case 'on_trial':
    case 'active':
    case 'past_due':
      return true;
    case 'cancelled':
      return !!sub.endsAt && nowIso < sub.endsAt;
    case 'paused':
    case 'unpaid':
    case 'expired':
      return false;
  }
}

// ── ΠΩΣ ΛΕΓΕΤΑΙ Η ΚΑΤΑΣΤΑΣΗ ΣΤΟΝ ΑΝΘΡΩΠΟ ──────────────────────────────────
//
// Η ΟΘΟΝΗ ΔΕΝ ΞΑΝΑΓΡΑΦΕΙ ΤΟ ΛΕΞΙΛΟΓΙΟ ΤΟΥ ΕΜΠΟΡΟΥ. Η κάρτα της συνδρομής
// συνέκρινε με συμβολοσειρές γραμμένες με το χέρι· μια αλλαγή παρόχου σημαίνει
// ότι καμία συνθήκη δεν ταιριάζει πια και ο πελάτης με ενεργή συνδρομή βλέπει
// κάρτα που δεν λέει τίποτα — χωρίς κανένα σφάλμα να το εξηγεί.
//
// Η μετάφραση γίνεται ΕΔΩ, δίπλα στη λίστα των καταστάσεων, ώστε μια νέα
// κατάσταση να μη γίνεται δεκτή χωρίς να αποφασιστεί τι λέει στον άνθρωπο.
export type SubPhase =
  /** Δοκιμή σε εξέλιξη. */
  | 'trial'
  /** Πληρωμένη και ενεργή. */
  | 'active'
  /** Μια ανανέωση απέτυχε και ξαναδοκιμάζεται. Η πρόσβαση μένει. */
  | 'retrying'
  /** Τελείωσε, πάγωσε, ή δεν πληρώθηκε ποτέ. */
  | 'lapsed'
  /** Καμία συνδρομή δεν έχει υπάρξει. */
  | 'none';

/**
 * Σε ποια φάση βρίσκεται η συνδρομή, από την κατάσταση που έγραψε ο webhook.
 *
 * ΤΟ `cancelled` ΔΕΝ ΕΙΝΑΙ ΦΑΣΗ ΕΔΩ, ΕΙΝΑΙ ΗΜΕΡΟΜΗΝΙΑ. Ακυρωμένη συνδρομή που
 * τρέχει ώς το `ends_at` δεν διαβάζεται από την κατάσταση αλλά από την ίδια την
 * ημερομηνία, που η οθόνη ελέγχει πρώτη.
 */
export function subPhase(status: string): SubPhase {
  if (!isMorStatus(status)) return 'none';
  switch (status) {
    case 'on_trial': return 'trial';
    case 'active': return 'active';
    case 'past_due': return 'retrying';
    case 'cancelled':
    case 'paused':
    case 'unpaid':
    case 'expired': return 'lapsed';
  }
}

// ═══════════════════════════════════════════════════════════════════════════
// ΑΝΑΒΑΘΜΙΣΗ Η ΥΠΟΒΑΘΜΙΣΗ, ΚΑΙ ΑΥΤΟ ΔΕΝ ΤΟ ΚΡΙΝΕΙ Ο ΕΜΠΟΡΟΣ
// ─────────────────────────────────────────────────────────────────────────
// Η σειρά των πακέτων είναι δική μας απόφαση και ζει στο PLAN_ORDER. Οσο αυτές
// οι τρεις συναρτήσεις έμεναν σε αρχείο με το όνομα του παρόχου, η διαδρομή
// αλλαγής πακέτου έπρεπε να εισάγει «lemon» για να ρωτήσει κάτι που δεν τον
// αφορά καθόλου.
// ═══════════════════════════════════════════════════════════════════════════

/** Τι είδους αλλαγή είναι αυτή. */
export type ChangeKind = 'upgrade' | 'downgrade' | 'same';

/** Πού βρίσκεται μια συνδρομή: ποιο πακέτο και με ποιον κύκλο. */
export interface PlanPoint { plan: PlanId; cycle: BillingCycle }

const rank = (p: PlanId): number => PLAN_ORDER.indexOf(p);

/**
 * ΤΟ «ΥΨΟΣ» ΕΝΟΣ ΣΗΜΕΙΟΥ: ΠΡΩΤΑ ΤΟ ΠΑΚΕΤΟ, ΚΑΙ ΜΕΤΑ Ο ΚΥΚΛΟΣ.
 *
 * Η ΣΕΙΡΑ ΒΓΑΙΝΕΙ ΑΠΟ ΤΟ PLAN_ORDER, ΟΧΙ ΑΠΟ ΤΗΝ ΤΙΜΗ. Η ετήσια συνδρομή του
 * «Ιδιοκτήτη» (42,90€) κοστίζει περισσότερο από τη μηνιαία του «Ιδιοκτήτη+»
 * (9,90€), οπότε μια σύγκριση ποσών θα έλεγε «υποβάθμιση» για κάτι που δίνει
 * ΠΕΡΙΣΣΟΤΕΡΑ. Το πακέτο κυριαρχεί· ο κύκλος λύνει μόνο την ισοπαλία.
 *
 * ΚΑΙ Ο ΚΥΚΛΟΣ ΜΕΤΡΑΕΙ, ΓΙΑΤΙ ΕΙΝΑΙ ΧΡΗΜΑΤΑ. Ιδιο πακέτο από μηνιαίο σε ετήσιο
 * σημαίνει «χρέωσέ με τώρα ολόκληρο τον χρόνο»· το αντίστροφο σημαίνει «μην
 * ξαναπάρεις τα λεφτά του χρόνου». Χωρίς αυτή τη διάκριση, μια αλλαγή κύκλου θα
 * περνούσε ως «τίποτα δεν άλλαξε» και ο πελάτης θα έμενε στην παλιά χρέωση.
 */
const height = (p: PlanPoint): number => rank(p.plan) * 2 + (p.cycle === 'annual' ? 1 : 0);

export function classifyChange(from: PlanPoint, to: PlanPoint): ChangeKind {
  if (rank(from.plan) < 0 || rank(to.plan) < 0) return 'same';
  const a = height(from), b = height(to);
  return a === b ? 'same' : b > a ? 'upgrade' : 'downgrade';
}

/**
 * Πέφτει το ΠΑΚΕΤΟ; Μόνο τότε κρατιέται κάτι ώς την ανανέωση.
 *
 * Η αλλαγή από ετήσιο σε μηνιαίο ΙΔΙΟΥ πακέτου είναι κι αυτή υποβάθμιση για
 * τον έμπορο —χρεώνει λιγότερα στην ανανέωση— αλλά δεν αφαιρεί καμία
 * δυνατότητα: δεν υπάρχει τίποτα να κρατηθεί.
 */
export function planDrops(from: PlanPoint, to: PlanPoint): boolean {
  const a = rank(from.plan), b = rank(to.plan);
  return a >= 0 && b >= 0 && b < a;
}

// ═══════════════════════════════════════════════════════════════════════════
// ΤΙ ΔΕΙΧΝΕΙ Η ΚΑΡΤΑ ΤΗΣ ΣΥΝΔΡΟΜΗΣ
// ─────────────────────────────────────────────────────────────────────────
// ΤΟ `endsAt` ΕΙΝΑΙ ΗΜΕΡΟΜΗΝΙΑ, ΟΧΙ ΣΗΜΑΙΑ. Η οθόνη το μετρούσε μόνο ως
// ΥΠΑΡΞΗ, οπότε το ίδιο πεδίο έλεγε δύο εντελώς διαφορετικά πράγματα με την
// ίδια πρόταση σε ενεστώτα: «η συνδρομή ισχύει ώς τις 12/09» σε κάποιον που
// έχει είκοσι ημέρες ακόμη και σε κάποιον που έληξε πέρσι.
//
// ΚΑΙ ΤΟ ΙΔΙΟ ΛΑΘΟΣ ΑΝΟΙΓΕ ΔΕΥΤΕΡΟ ΤΑΜΕΙΟ. Το «τρέχει συνδρομή;» κρινόταν από
// τη ΦΑΣΗ, που για την ακυρωμένη είναι «τελείωσε» — ενώ η ακυρωμένη τρέχει ώς
// την ημερομηνία της. Ο πελάτης που ακύρωσε την Τρίτη έβλεπε «Πληρωμή με
// κάρτα» πάνω από το πλαίσιο που του έλεγε ότι η συνδρομή του ισχύει· πατώντας
// το, αγόραζε δεύτερη δίπλα στην πρώτη.
//
// Ο ΚΑΝΟΝΑΣ ΖΕΙ ΕΔΩ ΚΑΙ ΟΧΙ ΣΤΟ .tsx. Γραμμένος μέσα στην οθόνη, καμία δοκιμή
// δεν τον έφτανε: χρειαζόταν περιηγητή για να κριθεί μια σύγκριση ημερομηνιών.
// ═══════════════════════════════════════════════════════════════════════════

/** Τι λέει το πλαίσιο πάνω από την κάρτα. */
export type CardTone =
  /** Ακυρώθηκε, τρέχει ακόμη ώς την ημερομηνία της. */
  | 'cancelled-running'
  /** Ακυρώθηκε και η ημερομηνία πέρασε. */
  | 'cancelled-over'
  | 'trial'
  | 'active'
  | 'retrying'
  /** Τίποτα να πει. */
  | 'none';

export interface CardState {
  tone: CardTone;
  /**
   * Τρέχει συνδρομή τη στιγμή αυτή; Τότε το ταμείο θα έφτιαχνε δεύτερη και το
   * κουμπί της πληρωμής δεν εμφανίζεται.
   */
  running: boolean;
}

export function cardState(sub: { status: string; endsAt: string | null }, nowIso: string): CardState {
  const endsAt = (sub.endsAt || '').trim();
  const endsAhead = !!endsAt && nowIso < endsAt;
  const phase = subPhase(sub.status);
  const live = phase === 'trial' || phase === 'active' || phase === 'retrying';
  const tone: CardTone = endsAhead ? 'cancelled-running'
    : endsAt ? 'cancelled-over'
      : phase === 'trial' ? 'trial'
        : phase === 'active' ? 'active'
          : phase === 'retrying' ? 'retrying'
            : 'none';
  return { tone, running: live || endsAhead };
}
