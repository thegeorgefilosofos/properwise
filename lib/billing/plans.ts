// ═══════════════════════════════════════════════════════════════════════════
// Πλάνα συνδρομής — ΜΙΑ πηγή αλήθειας (τιμές, όρια, δυνατότητες).
// Χρησιμοποιείται από: landing pricing, Ρυθμίσεις/Χρέωση, όριο ακινήτων,
// το ταμείο του εμπόρου. Καμία χρέωση δεν γίνεται εδώ· απλώς ορίζει τι επιτρέπει κάθε πλάνο.
// ═══════════════════════════════════════════════════════════════════════════


export type PlanId = 'free' | 'solo' | 'owner' | 'agency' | 'office';

/**
 * Ο κύκλος χρέωσης: μηνιαία ή ετήσια.
 *
 * ΗΤΑΝ ΓΡΑΜΜΕΝΟΣ ΔΥΟ ΦΟΡΕΣ — μία στα όρια του βοηθού και μία στον έμπορο —
 * με τις δύο δηλώσεις να μη γνωρίζουν η μία την άλλη. Ο κύκλος δεν ανήκει
 * ούτε στον βοηθό ούτε στον έμπορο: ανήκει στη συνδρομή, δηλαδή εδώ.
 */
export type BillingCycle = 'monthly' | 'annual';

/** Και οι δύο κύκλοι, με τη σειρά που εμφανίζονται σε κάθε επιλογέα. */
export const BILLING_CYCLES: readonly BillingCycle[] = ['monthly', 'annual'];

export interface Plan {
  id: PlanId;
  name: string;
  /**
   * Το ίδιο όνομα σε ΓΕΝΙΚΗ: «ένας μήνας Ιδιοκτήτη», «δύο μήνες Επαγγελματία».
   *
   * ΓΙΑΤΙ ΖΕΙ ΕΔΩ ΚΑΙ ΟΧΙ ΣΤΗΝ ΟΘΟΝΗ. Η κάρτα του Προγράμματος Πρόσκλησης
   * έγραφε «+1 μήνας Ιδιώτη» — πακέτο με αυτό το όνομα ΔΕΝ ΥΠΑΡΧΕΙ. Ο «Ιδιώτης»
   * είναι ο τρόπος χρήσης (Ιδιώτης / Επαγγελματίας) στις Ρυθμίσεις· το πακέτο
   * λέγεται «Ιδιοκτήτης». Ο χρήστης διάβαζε ανταμοιβή σε πακέτο που δεν θα
   * έβρισκε ποτέ στον τιμοκατάλογο.
   * Τα ελληνικά κλίνονται, οπότε το όνομα δεν αρκεί: αν μπει σε πρόταση ως
   * ονομαστική βγαίνει «μήνας Ιδιοκτήτης». Η πτώση ανήκει στην πηγή του
   * ονόματος, ώστε μια μετονομασία πακέτου να μη χρειάζεται κυνήγι σε οθόνες.
   */
  nameGen: string;
  /** Τιμή σε ευρώ. */
  priceMonthly: number;
  priceAnnual: number;
  /** Ακίνητα που ΠΕΡΙΛΑΜΒΑΝΕΙ η τιμή (Infinity = απεριόριστα). */
  maxProperties: number;
  /**
   * Μία σειρά, τρεις ως τέσσερις λέξεις, κάτω από το όνομα του πακέτου.
   *
   * ΤΙ ΤΟΥΣ ΕΛΕΙΠΕ. Οι τέσσερις ταμπέλες ήταν γραμμένες σε τέσσερις διαφορετικές
   * φωνές: μία μιλούσε για την εφορία, μία για σύγκριση, μία απαριθμούσε
   * επαγγέλματα και μία έλεγε τι ΔΕΝ έχει το πακέτο («χωρίς μετρητή»). Δίπλα
   * δίπλα διαβάζονταν ως τέσσερα προϊόντα, όχι ως τέσσερα σκαλιά του ίδιου.
   * Τώρα λένε όλες το ΑΠΟΤΕΛΕΣΜΑ, στην ίδια κλίμακα: ένα ακίνητο, πολλά, ομάδα,
   * χαρτοφυλάκιο. Όποιος τις ξαναγράψει, να κρατήσει το μήκος και τη φωνή.
   *
   * ΟΡΙΟ: 26 ΧΑΡΑΚΤΗΡΕΣ. Μετρημένο στον περιηγητή, στη στήλη 210 εικονοστοιχείων
   * της κάρτας τιμοκαταλόγου. Πάνω από αυτό η ταμπέλα σπάει σε δύο σειρές και
   * σπρώχνει το «Επαγγελματίας+» ένα σκαλί πιο κάτω από τα άλλα τρία.
   */
  tagline: string;
  features: string[];
  /** Ημέρες δωρεάν δοκιμής του πλάνου (0 = χωρίς δοκιμή). */
  trialDays: number;
}

// ═══════════════════════════════════════════════════════════════════════════
// ΤΑ ΕΠΙΠΛΕΟΝ ΑΚΙΝΗΤΑ ΤΩΝ 2,00€ ΒΓΗΚΑΝ, ΚΑΙ ΓΙ' ΑΥΤΟ ΔΕΝ ΥΠΑΡΧΕΙ ΤΙΠΟΤΑ ΕΔΩ
// ─────────────────────────────────────────────────────────────────────────
// Η ΠΡΟΤΑΣΗ ΗΤΑΝ ΓΡΑΜΜΕΝΗ ΠΑΝΤΟΥ ΚΑΙ ΑΓΟΡΑΣΤΗ ΠΟΥΘΕΝΑ. Οι Οροι χρήσης, η
// αρχική σελίδα, οι κάρτες δύο πακέτων και ο βοηθός έλεγαν ότι κάθε επιπλέον
// ακίνητο κοστίζει 2,00€ τον μήνα. Στην πραγματικότητα δεν υπήρχε διαδρομή
// αγοράς: η στήλη `billing_profiles.extra_properties` είναι server-owned, μένει
// μηδέν και ο χάρτης παραλλαγών του εμπόρου δεν έχει θέση για πρόσθετα.
//
// Δηλαδή τέσσερις επιφάνειες υπόσχονταν τιμή για κάτι που κανείς δεν μπορούσε
// να πάρει — και η πρώτη φορά που θα φαινόταν ήταν όταν ένας πληρωμένος
// πελάτης με τέσσερα ακίνητα ζητούσε το τέταρτο.
//
// ΤΟ ΟΡΙΟ ΕΙΝΑΙ ΠΙΑ ΟΡΙΟ. Οποιος χρειάζεται περισσότερα ακίνητα αλλάζει
// πακέτο και ο σύμβουλος πακέτου τον οδηγεί στο φθηνότερο που χωρά. Αν
// κάποτε φτιαχτεί η διαδρομή αγοράς, ξαναγράφεται εδώ μαζί με τον μηχανισμό —
// όχι πριν.
// ═══════════════════════════════════════════════════════════════════════════


/**
 * Ημέρες δωρεάν δοκιμής για κάθε νέο λογαριασμό.
 *
 * ΤΟ ΣΧΟΛΙΟ ΕΛΕΓΕ «στο πλάνο Ιδιοκτήτης» ΚΑΙ ΗΤΑΝ ΛΑΘΟΣ. Η `user_plan_rank`
 * της βάσης ανεβάζει τον νέο λογαριασμό σε rank 2, δηλαδή στο «Ιδιοκτήτης+»
 * (βλ. 20260805090000_solo_plan.sql) και το ίδιο δείχνουν οι Ρυθμίσεις και
 * οι Όροι χρήσης. Ένα σχόλιο που ονομάζει λάθος πακέτο στέλνει τον επόμενο
 * αναγνώστη να «διορθώσει» δύο σωστά κείμενα.
 */
export const TRIAL_DAYS = 30;

/**
 * Ημέρες που μένει ζωντανός ένας λογαριασμός ΧΩΡΙΣ συνδρομή.
 *
 * ΓΙΑΤΙ ΥΠΑΡΧΕΙ ΚΑΝ. Τα δεδομένα εδώ δεν είναι δικά μας: είναι ονόματα
 * ενοικιαστών, ΑΦΜ, μισθωτήρια και λογαριασμοί ρεύματος τρίτων ανθρώπων, που
 * ο ιδιοκτήτης τα εμπιστεύτηκε σε εμάς για έναν σκοπό. Οταν ο σκοπός πάψει —
 * δηλαδή όταν ο λογαριασμός δεν είναι πια πελάτης — παύει και ο λόγος να τα
 * κρατάμε. Το άρθρο 5 παρ. 1 στ. ε΄ του GDPR το ονομάζει «περιορισμό της
 * περιόδου αποθήκευσης» και δεν είναι προαιρετικό.
 *
 * Η ΒΑΣΗ ΤΟ ΞΕΡΕΙ ΚΙ ΑΥΤΗ, ως `public.account_grace_days()` και το
 * `scripts/db-replay.sh` συγκρίνει τα δύο σε κάθε εκτέλεση. Μια απόκλιση εδώ
 * σημαίνει σελίδα που υπόσχεται τριάντα ημέρες και σαρωτής που σβήνει σε
 * δεκαπέντε.
 */
export const ACCOUNT_GRACE_DAYS = 30;

// ═══════════════════════════════════════════════════════════════════════════
// ΚΑΘΕ ΔΥΝΑΤΟΤΗΤΑ ΧΩΡΑΕΙ ΣΕ ΜΙΑ ΓΡΑΜΜΗ, ΚΑΙ ΑΥΤΟ ΕΙΝΑΙ ΚΑΝΟΝΑΣ
// ─────────────────────────────────────────────────────────────────────────
// Η στήλη της κάρτας τιμοκαταλόγου είναι 192 εικονοστοιχεία, δηλαδή περίπου
// 29 χαρακτήρες στα 12px. Ό,τι ξεπερνά αυτό το όριο σπάει σε δύο ανισοβαρείς
// γραμμές («Δυνατότητα προσθήκης: 2€ ανά / ακίνητο τον μήνα») και μια λίστα
// από μισοσπασμένες γραμμές δεν διαβάζεται ως κατάλογος — διαβάζεται ως θόρυβος.
//
// Οι περιγραφές ήταν γραμμένες σαν προτάσεις («ΕΝΦΙΑ: υπολογισμός, έκπτωση
// ασφάλισης, δόσεις»), ενώ ο αναγνώστης εδώ ΣΑΡΩΝΕΙ, δεν διαβάζει: συγκρίνει
// τέσσερις στήλες με το μάτι. Κάθε γραμμή λέει ΕΝΑ πράγμα, με τις λέξεις που
// χρειάζονται και καμία παραπάνω.
//
// ΟΡΙΟ: 29 χαρακτήρες. Μετρημένο με scripts/probe-lines.mjs στο ".lp-plans
// .lp-even". Όποιος προσθέσει γραμμή, να ξαναμετρήσει.
// ═══════════════════════════════════════════════════════════════════════════
export const PLANS: Record<PlanId, Plan> = {
  // ═══════════════════════════════════════════════════════════════════════
  // ΤΟ «ΧΩΡΙΣ ΣΥΝΔΡΟΜΗ» ΔΕΝ ΕΙΝΑΙ ΠΡΟΪΟΝ, ΕΙΝΑΙ ΚΑΤΑΣΤΑΣΗ
  // ─────────────────────────────────────────────────────────────────────
  // Λεγόταν «Δωρεάν», με χαρακτηριστικό «Χωρίς χρονικό όριο» και η αρχική
  // σελίδα το πουλούσε ως «το πρώτο ακίνητο δωρεάν για πάντα». Δωρεάν είναι η
  // ΔΟΚΙΜΗ των τριάντα ημερών· μετά, το φθηνότερο πακέτο είναι 3,90€ τον μήνα.
  // Αυτή η βαθμίδα είναι πλέον ο σταθμός όπου κάθεται ο λογαριασμός ώσπου να
  // διαλέξει ο χρήστης πακέτο, ώστε τα δεδομένα του να μη χαθούν.
  // ═══════════════════════════════════════════════════════════════════════
  free: {
    id: 'free', name: 'Χωρίς συνδρομή', nameGen: 'χωρίς συνδρομή', priceMonthly: 0, priceAnnual: 0, maxProperties: 1, trialDays: 0,
    tagline: 'Ώσπου να διαλέξεις πακέτο',
    features: ['Τα δεδομένα σου δεν χάνονται', 'Χωρίς τα φορολογικά εργαλεία'],
  },
  // ═══════════════════════════════════════════════════════════════════════
  // ΤΕΣΣΕΡΑ ΠΑΚΕΤΑ, ΔΥΟ ΟΙΚΟΓΕΝΕΙΕΣ, ΕΝΑ ΟΝΟΜΑ ΠΟΥ ΛΕΕΙ ΠΟΙΟΣ ΕΙΣΑΙ
  // ─────────────────────────────────────────────────────────────────────
  // Τα ονόματα ήταν τέσσερα ασύνδετα ουσιαστικά: «Ένα ακίνητο», «Ιδιοκτήτης»,
  // «Επαγγελματίας», «Γραφείο». Ο χρήστης δεν διάβαζε κλίμακα, διάβαζε τέσσερα
  // διαφορετικά προϊόντα και δεν καταλάβαινε ποιο είναι το επόμενο βήμα του.
  // Χειρότερα: το προφίλ της εφαρμογής λέει «Ιδιώτης» ή «Επαγγελματίας» και
  // κανένα από τα δύο δεν αντιστοιχούσε σε όνομα πακέτου.
  //
  // Δύο οικογένειες, δύο σκαλιά η καθεμία, ίδιες λέξεις με το προφίλ:
  //     Ιδιοκτήτης → Ιδιοκτήτης+        Επαγγελματίας → Επαγγελματίας+
  // Ο χρήστης ξέρει αμέσως σε ποια οικογένεια ανήκει και το «+» λέει από μόνο
  // του ότι υπάρχει επόμενο σκαλί χωρίς να υποτιμά το προηγούμενο.
  //
  // ΤΟ ΑΚΙΝΗΤΟ ΔΕΝ ΕΙΝΑΙ Ο ΜΟΝΟΣ ΔΙΑΧΩΡΙΣΜΟΣ. Αν η μόνη διαφορά ήταν το πλήθος,
  // ο χρήστης με ένα ακίνητο δεν θα είχε ποτέ λόγο να ανέβει και το πακέτο θα
  // ήταν μετρητής, όχι προϊόν. Κάθε σκαλί προσθέτει εργαλεία που λύνουν άλλο
  // πρόβλημα (βλ. FEATURE_MIN_PLAN στο entitlements.ts) και μεγαλύτερο πακέτο
  // ερωτήσεων στον βοηθό (βλ. aiLimits.ts).
  // ═══════════════════════════════════════════════════════════════════════

  // ── ΙΔΙΟΚΤΗΤΗΣ (3,90€) ────────────────────────────────────────────────
  // Ο ΜΕΓΑΛΥΤΕΡΟΣ ΧΡΗΣΤΗΣ ΤΗΣ ΕΦΑΡΜΟΓΗΣ ΕΙΧΕ ΤΗ ΧΕΙΡΟΤΕΡΗ ΕΠΙΛΟΓΗ. Ο Έλληνας
  // με ΕΝΑ διαμέρισμα —η συντριπτική πλειοψηφία— πλήρωνε τριπλάσια
  // χωρητικότητα για να πάρει τα φορολογικά που τον αφορούν. Στα 3,90€ τα
  // παίρνει όλα για το ένα του σπίτι: λιγότερο από έναν καφέ τον μήνα και
  // συγκρίνεται με το κόστος ενός λάθους σε μία δήλωση.
  //
  // ΓΙΑΤΙ 3,90 ΚΑΙ ΟΧΙ 4: ο χρήστης εδώ δεν κάνει πράξεις στο κεφάλι του·
  // κοιτάζει μία τιμή και αποφασίζει αν είναι μικρή. Το 3,90 διαβάζεται «κάτω
  // από τέσσερα». Η ίδια λογική με το 9,90.
  //
  // ΚΑΙ ΓΙΑΤΙ Η ΕΤΗΣΙΑ ΔΙΝΕΙ ΕΝΑΝ ΜΗΝΑ, ΟΧΙ ΔΥΟ: στα 39€ (δέκα μήνες) η
  // έκπτωση ήταν 7,80€ — δηλαδή διπλάσια σε απόλυτο ποσό από το ΣΥΝΟΛΟ που
  // κερδίζει ο συνδρομητής του πακέτου σε έναν μήνα. Το φθηνότερο πακέτο δεν
  // αντέχει τη μεγαλύτερη έκπτωση της κλίμακας: 42,90€ είναι έντεκα μήνες,
  // ένας δώρο και η προπληρωμή εξακολουθεί να συμφέρει.
  solo: {
    id: 'solo', name: 'Ιδιοκτήτης', nameGen: 'Ιδιοκτήτη', priceMonthly: 3.9, priceAnnual: 42.9, maxProperties: 1, trialDays: TRIAL_DAYS,
    tagline: 'Το ακίνητό σου σε τάξη',
    features: [
      'Ε2 έτοιμο για τον λογιστή',
      'Έλεγχος Δήλωσης Μίσθωσης',
      'ΕΝΦΙΑ με έκπτωση και δόσεις',
      'Ενοικιαστής και εισπράξεις',
      'Σάρωση με φωτογραφία',
    ],
  },

  // ── ΙΔΙΟΚΤΗΤΗΣ+ (9,90€) ───────────────────────────────────────────────
  // Το σκαλί που ανοίγει τη ΣΥΓΚΡΙΣΗ. Με ένα ακίνητο δεν υπάρχει τι να
  // συγκρίνεις· από το δεύτερο και πάνω, το «ποιο μου αποδίδει και ποιο με
  // τρώει» είναι η ερώτηση που δεν απαντά κανένα φύλλο Excel.
  owner: {
    id: 'owner', name: 'Ιδιοκτήτης+', nameGen: 'Ιδιοκτήτη+', priceMonthly: 9.9, priceAnnual: 99, maxProperties: 3, trialDays: TRIAL_DAYS,
    tagline: 'Πολλά ακίνητα, μία εικόνα',
    features: [
      'Έως 3 ακίνητα',
      'Σύγκριση απόδοσης ανά ακίνητο',
      // Η ΓΡΑΜΜΗ «MOBILE APP 15 ΜΕΡΕΣ ΝΩΡΙΤΕΡΑ» ΕΦΥΓΕ. Πουλούσε πρόωρη πρόσβαση
      // σε προϊόν που δεν υπάρχει και δεν έχει ημερομηνία: στον κώδικα υπάρχει
      // μόνο λίστα αναμονής. Χαρακτηριστικό πληρωμένου πακέτου του οποίου όλη η
      // αξία είναι μια υπόσχεση χωρίς ημερομηνία δεν είναι χαρακτηριστικό.
    ],
  },

  // ── ΕΠΑΓΓΕΛΜΑΤΙΑΣ (24,90€) ────────────────────────────────────────────
  // Εδώ αλλάζει το ερώτημα: δεν είναι «τι πληρώνω», είναι «τι λέω στον πελάτη
  // μου». Πελατολόγιο, ομάδα, επώνυμες αναφορές και επενδυτική ανάλυση.
  agency: {
    id: 'agency', name: 'Επαγγελματίας', nameGen: 'Επαγγελματία', priceMonthly: 24.9, priceAnnual: 249, maxProperties: 15, trialDays: TRIAL_DAYS,
    tagline: 'Ομάδα, ρόλοι και λογοδοσία',
    features: [
      'Έως 15 ακίνητα',
      'Πελατολόγιο και χαρτοφυλάκιο',
      // ΤΟ «ΧΩΡΙΣ ΟΡΙΟ ΧΡΗΣΤΩΝ» ΗΤΑΝ ΓΡΑΜΜΕΝΟ ΣΤΟ ΑΚΡΙΒΟΤΕΡΟ ΠΑΚΕΤΟ, ΣΑΝ ΝΑ ΤΟ
      // ΠΡΟΣΘΕΤΕ. Δεν το προσθέτει: κανένα πακέτο δεν έχει όριο χρηστών, σε
      // κανένα σημείο του κώδικα ή της βάσης. Η ομάδα ξεκινά εδώ και εδώ είναι
      // αλήθεια ότι δεν μετράει κεφάλια.
      'Ομάδα με ρόλους, χωρίς όριο',
      'Αναφορές με την επωνυμία σου',
      'Λογιστικό ημερολόγιο, τράπεζα',
      'Ανάλυση IRR, NPV και DSCR',
    ],
  },

  // ── ΕΠΑΓΓΕΛΜΑΤΙΑΣ+ (79,90€) ───────────────────────────────────────────
  // Δεν αγοράζει δυνατότητες, αγοράζει να μη μετράει: ακίνητα, χρήστες και
  // ερωτήσεις χωρίς μετρητή και άνθρωπο στο τηλέφωνο.
  office: {
    id: 'office', name: 'Επαγγελματίας+', nameGen: 'Επαγγελματία+', priceMonthly: 79.9, priceAnnual: 799, maxProperties: Infinity, trialDays: TRIAL_DAYS,
    tagline: 'Επαγγελματικό χαρτοφυλάκιο',
    // ΔΥΟ ΑΠΟ ΤΙΣ ΤΕΣΣΕΡΙΣ ΓΡΑΜΜΕΣ ΔΕΝ ΠΡΟΣΘΕΤΑΝ ΤΙΠΟΤΑ, ΚΑΙ Η ΚΑΡΤΑ ΛΕΕΙ ΑΠΟ
    // ΠΑΝΩ «ΟΛΑ ΤΟΥ ΕΠΑΓΓΕΛΜΑΤΙΑ, ΚΑΙ:». Το «Ομάδα χωρίς όριο χρηστών» ίσχυε ήδη
    // στον Επαγγελματία — κανένα πακέτο δεν μετράει χρήστες πουθενά — και το
    // «Κάθε νέα δυνατότητα πρώτα» ξεκινά από τον «Ιδιοκτήτης+»
    // (FEATURE_MIN_PLAN.early_access), όπως γράφουν και οι Όροι χρήσης.
    // Μια λίστα «τι παραπάνω παίρνω» που περιέχει όσα έχω ήδη δεν πουλάει το
    // πακέτο· δείχνει ότι δεν ξέρουμε τι πουλάμε.
    features: [
      'Ακίνητα χωρίς όριο και χρέωση',
      // Η ΣΕΙΡΑ ΔΙΑΘΕΣΗΣ ΕΙΝΑΙ ΠΡΑΓΜΑΤΙΚΟ ΠΡΟΝΟΜΙΟ, ΑΡΚΕΙ ΝΑ ΤΗΡΕΙΤΑΙ.
      // Η πρόωρη πρόσβαση ξεκινά από το «Ιδιοκτήτης+» (FEATURE_MIN_PLAN.
      // early_access) — εδώ δεν λέμε «έχεις πρόσβαση», λέμε ότι είσαι ΠΡΩΤΟΣ
      // στη σειρά. Είναι δέσμευση κυκλοφορίας, γραμμένη και στους Όρους
      // χρήσης, όχι κλείδωμα δυνατότητας: αν σπάσει, σπάει εκεί.
      'Πρώτος σε κάθε νέα δυνατότητα',
      'Άμεση επικοινωνία μαζί μας',
    ],
  },
};

// Η ΣΕΙΡΑ ΕΙΝΑΙ Η ΚΑΤΑΤΑΞΗ, ΚΑΙ Η ΚΑΤΑΤΑΞΗ ΖΕΙ ΚΑΙ ΣΤΗ ΒΑΣΗ.
// Το `rank()` του entitlements.ts είναι το `indexOf` αυτού του πίνακα και ΠΡΕΠΕΙ
// να ταυτίζεται με την `public.user_plan_rank`. Παρεμβολή πλάνου εδώ χωρίς
// αντίστοιχο migration μετακινεί κάθε επόμενο επίπεδο κατά ένα — σιωπηλά, με
// αποτέλεσμα ο συνδρομητής «Ιδιοκτήτης» να διαβάζεται ως «Ένα ακίνητο».
// Το `solo` μπήκε μαζί με το 20260805090000_solo_plan.sql.
export const PLAN_ORDER: PlanId[] = ['free', 'solo', 'owner', 'agency', 'office'];

/** Ασφαλής ανάγνωση πλάνου από τυχόν αποθηκευμένο id (fallback: free). */
export function normalizePlan(id: string | null | undefined): PlanId {
  return id === 'solo' || id === 'owner' || id === 'agency' || id === 'office' ? id : 'free';
}

/**
 * Χρεώνεται πραγματικά αυτό το πακέτο;
 *
 * ΔΕΝ ΤΑΥΤΙΖΕΤΑΙ ΜΕ ΤΟ «ΕΝΕΡΓΟ ΕΠΙΠΕΔΟ». Η δοκιμή, οι δωρεάν μήνες και η
 * ιδιότητα Συνεργάτη ανεβάζουν το επίπεδο χωρίς να μπαίνει ευρώ στο ταμείο.
 * Ελέγχεται η ΤΙΜΗ και όχι μια γραμμένη με το χέρι λίστα ονομάτων, ώστε ένα
 * νέο πακέτο να μη χρειάζεται να θυμηθεί κανείς να το προσθέσει εδώ.
 *
 * Ιδιο σύνολο με τη λίστα της `bump_ai_usage` στη βάση.
 */
export function isPayingPlan(id: string | null | undefined): boolean {
  return PLANS[normalizePlan(id)].priceMonthly > 0;
}

// ΤΟ `planLimit` ΕΦΥΓΕ ΚΙ ΑΥΤΟ, ΓΙΑ ΤΟΝ ΙΔΙΟ ΛΟΓΟ ΜΕ ΤΟ `canAddProperty`.
// Επέστρεφε το `maxProperties` του πακέτου και ΜΟΝΟ αυτό: σε χρήστη που είχε
// πληρώσει για επιπλέον ακίνητα έδινε μικρότερο όριο από το πραγματικό. Εμεινε
// πίσω όταν έφυγε το `canAddProperty` και το βρήκε ο φύλακας νεκρών εξαγωγών,
// γιατί το καλούσε μόνο το δικό του τεστ. Η μία απάντηση είναι το
// `propertyLimit` του entitlements.ts.

// ΤΟ `canAddProperty` ΕΦΥΓΕ ΑΠΟ ΕΔΩ.
// Υπήρχε σε δύο αρχεία με το ίδιο όνομα και ΔΙΑΦΟΡΕΤΙΚΗ απάντηση: αυτό εδώ
// κοιτούσε μόνο το πλάνο, ενώ το lib/billing/entitlements.ts μετράει και τα
// ΑΓΟΡΑΣΜΕΝΑ επιπλέον ακίνητα. Δηλαδή σε χρήστη που είχε πληρώσει για παραπάνω,
// αυτό εδώ έλεγε «όχι». Καμία οθόνη δεν το καλούσε — μόνο το ίδιο του το test.
// Η μία απάντηση δίνεται από το `canAddProperty` του entitlements.ts, που
// συμφωνεί με τον έλεγχο `enforce_property_limit` της βάσης.

/** Το μικρότερο πλάνο που χωράει τόσα ακίνητα ΧΩΡΙΣ αγορά επιπλέον. */
export function planForCount(count: number): PlanId {
  for (const id of PLAN_ORDER) if (count <= PLANS[id].maxProperties) return id;
  return 'office';
}

/** Μηνιαίο ισοδύναμο ετήσιας τιμής (για εμφάνιση «X/μήνα με ετήσια»). */
export function annualPerMonth(id: PlanId): number {
  return Math.round((PLANS[id].priceAnnual / 12) * 100) / 100;
}

// ── Το όριο ακινήτων ───────────────────────────────────────────────────────

/** Πόσα ακίνητα επιτρέπει το πακέτο. */
export function propertyAllowance(planId: string | null | undefined): number {
  const plan = PLANS[normalizePlan(planId)];
  return Number.isFinite(plan.maxProperties) ? plan.maxProperties : Infinity;
}

// ═══════════════════════════════════════════════════════════════════════════
// Ο ΣΥΜΒΟΥΛΟΣ ΠΑΚΕΤΟΥ ΔΙΑΓΡΑΦΗΚΕ, ΚΑΙ ΑΞΙΖΕΙ ΝΑ ΓΡΑΦΤΕΙ ΓΙΑΤΙ
// ─────────────────────────────────────────────────────────────────────────
// Εδώ ζούσαν τέσσερις εξαγόμενες συναρτήσεις —`monthlyPrice`, `cheapestFor`,
// `planAdvice`, `propertyLimitLabel`— και η `planAdvice` έφερε πάνω της το
// σχόλιο «το πιο σημαντικό κομμάτι αυτού του αρχείου». ΚΑΜΙΑ ΤΟΥΣ ΔΕΝ
// ΚΑΛΟΥΝΤΑΝ ΑΠΟ ΚΑΜΙΑ ΟΘΟΝΗ. Υπήρχαν, δοκιμάζονταν και δεν έβγαιναν ποτέ
// μπροστά σε χρήστη.
//
// ΚΑΙ ΑΝ ΕΒΓΑΙΝΑΝ, ΘΑ ΕΚΑΝΑΝ ΖΗΜΙΑ. Ο λόγος ύπαρξης της `planAdvice` ήταν τα
// επιπλέον ακίνητα των 2,00€: με οκτώ πρόσθετα ο «Ιδιοκτήτης+» κόστιζε
// 25,90€ ενώ ο «Επαγγελματίας» 24,90€ και ήταν σωστό να το πούμε εμείς.
// Μόλις έφυγαν τα πρόσθετα, το μόνο που έμεινε να συγκρίνει ήταν το ΠΛΗΘΟΣ
// ΑΚΙΝΗΤΩΝ — οπότε θα έλεγε σε επαγγελματία με δύο ακίνητα και ενεργό
// Πελατολόγιο «γύρνα στον Ιδιοκτήτη+ και γλίτωσε 70,00€», δηλαδή θα του
// πρότεινε να χάσει τις καρτέλες για τις οποίες πληρώνει.
//
// Μια συνάρτηση που δεν καλείται και που, αν κληθεί, δίνει λάθος απάντηση,
// δεν είναι υποδομή· είναι παγίδα με σχόλιο που την επαινεί. Αν χρειαστεί
// ξανά σύμβουλος, θα γραφτεί ξέροντας ΚΑΙ τις δυνατότητες, όχι μόνο το όριο.
// ═══════════════════════════════════════════════════════════════════════════
