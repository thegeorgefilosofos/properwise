// ═══════════════════════════════════════════════════════════════════════════
// ΜΙΑ ΔΙΕΥΘΥΝΣΗ ΓΙΑ ΟΛΑ ΤΑ EMAIL
// ─────────────────────────────────────────────────────────────────────────
// Δεκαοκτώ συναρτήσεις άκρης στέλνουν συνδέσμους σε ανθρώπους. Το domain ήταν
// γραμμένο σε καθεμία χωριστά και είχε ήδη αποκλίνει σε ΤΡΕΙΣ τιμές:
//
//   `https://properwise.gr`             σε έξι συναρτήσεις — domain που ΔΕΝ
//                                       έχει αγοραστεί, δηλαδή σύνδεσμος που
//                                       δεν απαντά μέσα σε email πελάτη.
//   ένας παλιός host του Vercel σε τρεις — άλλη διεύθυνση.
//   και η πραγματική διεύθυνση παραγωγής, σε καμία.
//
// Ενας ιδιοκτήτης που πατούσε «Άνοιγμα PROPERWISE» σε υπενθύμιση κατέληγε
// αλλού από έναν που πατούσε πρόσκληση ομάδας. Το ένα από τα δύο ήταν σπασμένο,
// και κανένα δεν ήταν το σωστό.
//
// Η τιμή έρχεται από το `APP_URL` του περιβάλλοντος της συνάρτησης. Η
// προεπιλογή είναι η διεύθυνση που ΑΠΑΝΤΑ σήμερα, όχι αυτή που σχεδιάζεται.
// ═══════════════════════════════════════════════════════════════════════════

/** Η προεπιλογή όταν λείπει το APP_URL: το κανονικό, ζωντανό domain. Ο παλιός
 *  host του Vercel έπαψε να απαντά μετά τη μετονομασία του project — και άφηνε
 *  σπασμένες εικόνες λογότυπου στα email. Το properwise.gr απαντά σήμερα. */
const FALLBACK = 'https://properwise.gr'

/** Καθαρίζει την τιμή: χωρίς κάθετο στο τέλος, μόνο https, αλλιώς προεπιλογή. */
export function normalizeSite(raw: string | undefined | null): string {
  const v = (raw || '').trim().replace(/\/+$/, '')
  return /^https:\/\/[^/\s]+$/.test(v) ? v : FALLBACK
}

/**
 * Η τιμή του περιβάλλοντος, με τρόπο που δουλεύει και εκτός Deno.
 *
 * ΤΟ ΙΔΙΟ ΑΡΧΕΙΟ ΔΙΑΒΑΖΕΤΑΙ ΑΠΟ ΔΥΟ ΧΡΟΝΟΥΣ ΕΚΤΕΛΕΣΗΣ. Οι συναρτήσεις τρέχουν
 * σε Deno, αλλά τα σενάρια επαλήθευσης (`verify-*.ts`) τρέχουν σε Node μαζί με
 * τις υπόλοιπες σουίτες. Ενα σκέτο `Deno.env.get` έριξε δύο από αυτές με
 * «Deno is not defined» — και το `try` καλύπτει επιπλέον την περίπτωση όπου το
 * Deno τρέχει χωρίς δικαίωμα ανάγνωσης περιβάλλοντος.
 */
function envUrl(): string | undefined {
  try {
    const g = globalThis as { Deno?: { env?: { get(k: string): string | undefined } } }
    return g.Deno?.env?.get('APP_URL')
  } catch { return undefined }
}

/** Η διεύθυνση της εφαρμογής, όπως τη βλέπει ο παραλήπτης ενός email. */
export const APP_URL = normalizeSite(envUrl())

/** Πλήρης διεύθυνση μιας σελίδας. Οι διαδρομές ξεκινούν με κάθετο. */
export const appUrl = (path = ''): string => APP_URL + path
